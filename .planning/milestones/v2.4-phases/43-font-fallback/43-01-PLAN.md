---
phase: 43-font-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_renderer/src/grid_renderer.rs
autonomous: true
requirements:
  - FONT-01
  - FONT-02

must_haves:
  truths:
    - "CJK characters produce shaped glyphs via fallback when primary font lacks them"
    - "Multi-script characters (Arabic, Cyrillic, CJK) all produce glyphs, not tofu"
    - "Fallback glyphs respect set_monospace_width constraint for grid alignment"
    - "build_cell_buffers correctly handles CJK cells with WIDE_CHAR flag through fallback"
  artifacts:
    - path: "crates/glass_renderer/src/grid_renderer.rs"
      provides: "Font fallback validation tests"
      contains: "fallback_renders_cjk_glyph"
  key_links:
    - from: "FontSystem::new()"
      to: "Shaping::Advanced"
      via: "cosmic-text font fallback pipeline"
      pattern: "Shaping::Advanced"
    - from: "buffer.set_monospace_width"
      to: "fallback glyph width"
      via: "monospace width constraint on fallback glyphs"
      pattern: "set_monospace_width.*Some\\(buf_width\\)"
---

<objective>
Validate and test that font fallback works correctly for missing glyphs via the existing cosmic-text/glyphon pipeline.

Purpose: The existing code already uses Shaping::Advanced and FontSystem::new() which enable automatic font fallback. This plan validates that fallback actually works -- CJK and other non-Latin glyphs render correctly and align to the monospace grid. No new dependencies or architectural changes needed.

Output: 4 new unit tests in grid_renderer.rs proving font fallback works for FONT-01 and FONT-02 requirements.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-font-fallback/43-RESEARCH.md

<interfaces>
<!-- Key types and contracts from crates/glass_renderer/src/grid_renderer.rs -->

```rust
pub struct GridRenderer {
    pub cell_width: f32,
    pub cell_height: f32,
    font_size: f32,
    scale_factor: f32,
    pub font_family: String,
}

impl GridRenderer {
    pub fn new(font_system: &mut FontSystem, font_family: &str, font_size: f32, scale_factor: f32) -> Self;
    pub fn build_cell_buffers(&self, font_system: &mut FontSystem, snapshot: &GridSnapshot, buffers: &mut Vec<Buffer>, positions: &mut Vec<(usize, i32)>);
}

// Test helpers already available:
fn make_cell(c: char, col: usize, line: i32, flags: Flags) -> glass_terminal::RenderedCell;
fn make_snapshot(cells: Vec<glass_terminal::RenderedCell>, columns: usize) -> GridSnapshot;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add font fallback validation tests (FONT-01)</name>
  <files>crates/glass_renderer/src/grid_renderer.rs</files>
  <behavior>
    - Test fallback_renders_cjk_glyph: Create a GridRenderer with "monospace" font, shape CJK character U+4E16 via Buffer with Shaping::Advanced. Assert layout_runs().next() is Some and glyphs is non-empty (cosmic-text found a fallback font).
    - Test fallback_renders_multi_script: Same approach for Arabic U+0639, Cyrillic U+0414, CJK U+4E16, and Thai U+0E01. Each must produce at least one layout run with at least one glyph.
  </behavior>
  <action>
Add two tests to the existing `#[cfg(test)] mod tests` block in grid_renderer.rs:

1. `fallback_renders_cjk_glyph` -- Create FontSystem::new() and GridRenderer::new with "monospace" font at 14.0/1.0. Create a Buffer with Metrics::new(14.0, renderer.cell_height), set_size to (cell_width*2.0, cell_height), set_monospace_width to Some(cell_width*2.0), set_text with U+4E16 using Attrs::new().family(Family::Name("monospace")) and Shaping::Advanced, shape_until_scroll. Assert layout_runs().next().is_some() and first run's glyphs is non-empty.

2. `fallback_renders_multi_script` -- Same pattern but loop over chars: [('\u{0639}', "Arabic"), ('\u{0414}', "Cyrillic"), ('\u{4E16}', "CJK"), ('\u{0E01}', "Thai")]. Each should produce at least one glyph. Use cell_width (not 2x) for non-CJK chars. For CJK use 2x. Assert with descriptive message including script name.

Use the existing test patterns (FontSystem::new, GridRenderer::new). Place tests after the existing test functions.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo test -p glass_renderer fallback_renders -- --exact 2>&1</automated>
  </verify>
  <done>Both tests pass, proving cosmic-text font fallback resolves CJK and multi-script glyphs on the system</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fallback grid alignment tests (FONT-02)</name>
  <files>crates/glass_renderer/src/grid_renderer.rs</files>
  <behavior>
    - Test fallback_glyph_respects_monospace_width: Shape CJK U+4E16 in a Buffer with set_monospace_width(Some(buf_width)) where buf_width = cell_width * 2.0. Assert the first glyph's width (glyph.w) is within 1.0 of buf_width.
    - Test build_cell_buffers_handles_cjk_fallback: Create a snapshot with cells ['A' at col 0 (normal), U+4E16 at col 1 (WIDE_CHAR flag), spacer at col 2 (WIDE_CHAR_SPACER flag), 'B' at col 3 (normal)]. Call build_cell_buffers. Assert 3 buffers produced (A, CJK, B -- spacer skipped). Assert the CJK buffer (index 1) has at least one layout run with a non-empty glyphs vec.
  </behavior>
  <action>
Add two tests to the existing test module:

1. `fallback_glyph_respects_monospace_width` -- Create FontSystem and GridRenderer with "monospace"/14.0/1.0. Build a Buffer with Metrics::new(14.0, renderer.cell_height), set_size(Some(renderer.cell_width * 2.0), Some(renderer.cell_height)), set_monospace_width(Some(renderer.cell_width * 2.0)), set_text with U+4E16 and Shaping::Advanced, shape_until_scroll. Get first glyph from layout_runs, assert (glyph.w - buf_width).abs() < 1.0 with descriptive message.

2. `build_cell_buffers_handles_cjk_fallback` -- Use existing make_cell and make_snapshot helpers. Create cells: make_cell('A', 0, 0, Flags::empty()), make_cell('\u{4E16}', 1, 0, Flags::WIDE_CHAR), make_cell(' ', 2, 0, Flags::WIDE_CHAR_SPACER), make_cell('B', 3, 0, Flags::empty()). Make snapshot with columns=4. Call build_cell_buffers. Assert buffers.len() == 3. Assert the second buffer (CJK) has layout_runs().next().is_some() and glyphs non-empty.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo test -p glass_renderer fallback_glyph_respects_monospace_width build_cell_buffers_handles_cjk_fallback 2>&1</automated>
  </verify>
  <done>Both tests pass, proving fallback glyphs are constrained to monospace grid width and build_cell_buffers correctly processes CJK fallback cells</done>
</task>

</tasks>

<verification>
All 4 font fallback tests pass:
```bash
cargo test -p glass_renderer --lib
```
Full workspace tests still pass:
```bash
cargo test --workspace
```
Clippy clean:
```bash
cargo clippy --workspace -- -D warnings
```
</verification>

<success_criteria>
- 4 new tests in grid_renderer.rs all pass (fallback_renders_cjk_glyph, fallback_renders_multi_script, fallback_glyph_respects_monospace_width, build_cell_buffers_handles_cjk_fallback)
- No regressions in existing test suite
- FONT-01 validated: missing glyphs produce shaped layout runs via cosmic-text fallback
- FONT-02 validated: fallback glyphs constrained to correct monospace width within cell grid
</success_criteria>

<output>
After completion, create `.planning/phases/43-font-fallback/43-01-SUMMARY.md`
</output>
