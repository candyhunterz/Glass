---
phase: 23-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_mux/src/tab.rs
  - crates/glass_mux/src/session_mux.rs
autonomous: true
requirements: [TAB-01, TAB-02, TAB-03]

must_haves:
  truths:
    - "SessionMux can add a new tab with a session"
    - "SessionMux can close a tab by index and return the removed session"
    - "SessionMux can activate a tab by index"
    - "SessionMux can cycle next/prev tabs with wraparound"
    - "Closing a middle tab adjusts active_tab correctly"
    - "Tab has a title field for display"
  artifacts:
    - path: "crates/glass_mux/src/session_mux.rs"
      provides: "Tab CRUD methods: add_tab, close_tab, activate_tab, next_tab, prev_tab, tab_count, tabs()"
      contains: "fn add_tab"
    - path: "crates/glass_mux/src/tab.rs"
      provides: "Tab struct with title field"
      contains: "pub title"
  key_links:
    - from: "crates/glass_mux/src/session_mux.rs"
      to: "crates/glass_mux/src/tab.rs"
      via: "Tab struct used in tabs Vec"
      pattern: "Tab"
---

<objective>
Add full tab lifecycle methods to SessionMux and a title field to Tab.

Purpose: Enable multi-tab management in SessionMux so that main.rs (Plan 03) can create, close, switch, and cycle tabs. This is the core data model for tabs.
Output: SessionMux with add_tab, close_tab, activate_tab, next_tab, prev_tab methods. Tab with title field. Comprehensive unit tests.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tabs/23-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From crates/glass_mux/src/types.rs:
```rust
pub use glass_core::event::SessionId;

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub struct TabId(u64);
impl TabId {
    pub fn new(n: u64) -> Self { Self(n) }
    pub fn val(self) -> u64 { self.0 }
}
```

From crates/glass_mux/src/tab.rs (current):
```rust
pub struct Tab {
    pub id: TabId,
    pub session_id: SessionId,
}
```

From crates/glass_mux/src/session_mux.rs (current):
```rust
pub struct SessionMux {
    sessions: HashMap<SessionId, Session>,
    tabs: Vec<Tab>,
    active_tab: usize,
    next_id: u64,
}
// Has: new(), focused_session(), focused_session_mut(), session(), session_mut(),
//      focused_session_id(), next_session_id()
```

From crates/glass_mux/src/session.rs:
```rust
pub struct Session {
    pub id: SessionId,
    pub pty_sender: PtySender,
    pub term: Arc<FairMutex<Term<EventProxy>>>,
    pub title: String,
    // ... many other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add title field to Tab and tab CRUD methods to SessionMux</name>
  <files>crates/glass_mux/src/tab.rs, crates/glass_mux/src/session_mux.rs</files>
  <behavior>
    - Test: add_tab inserts tab after active_tab, sets new tab as active, returns TabId
    - Test: add_tab to empty mux (after closing all) works correctly
    - Test: close_tab removes tab and returns Session, adjusts active_tab if needed
    - Test: close_tab on last remaining tab returns Session, leaves tabs empty
    - Test: close_tab on middle tab (e.g., index 1 of 3) with active_tab=2 adjusts active_tab to 1
    - Test: close_tab with out-of-bounds index returns None
    - Test: activate_tab sets active_tab when index is valid, no-ops when invalid
    - Test: next_tab cycles forward with wraparound (last -> first)
    - Test: prev_tab cycles backward with wraparound (first -> last)
    - Test: tab_count returns correct count after add/close operations
    - Test: tabs() returns slice of all tabs in order
    - Test: active_tab_index() returns current active tab index
  </behavior>
  <action>
    1. **Modify `crates/glass_mux/src/tab.rs`:** Add `pub title: String` field to Tab struct. Update doc comment to note the title is derived from CWD or process name.

    2. **Update `SessionMux::new()` in `crates/glass_mux/src/session_mux.rs`:** The Tab construction in `new()` currently does not set a title field. Add `title: session.title.clone()` (or a default "Glass" string) to the Tab initialization.

    3. **Add the following methods to `SessionMux`** (follow the exact signatures from the RESEARCH.md Pattern 1):
       - `pub fn add_tab(&mut self, session: Session) -> TabId` -- Insert tab after active_tab position, set it as active. Use `self.next_id` for TabId, increment counter. Insert session into sessions HashMap. Set tab title from `session.title.clone()`.
       - `pub fn close_tab(&mut self, index: usize) -> Option<Session>` -- Remove tab at index, remove session from HashMap, adjust active_tab if it's >= tabs.len(). Return the removed Session for cleanup.
       - `pub fn activate_tab(&mut self, index: usize)` -- Set active_tab if index is valid.
       - `pub fn next_tab(&mut self)` -- Cycle active_tab forward with modulo wraparound.
       - `pub fn prev_tab(&mut self)` -- Cycle active_tab backward with wraparound.
       - `pub fn tab_count(&self) -> usize` -- Return tabs.len().
       - `pub fn active_tab_index(&self) -> usize` -- Return active_tab.
       - `pub fn tabs(&self) -> &[Tab]` -- Return &self.tabs.

    4. **Unit tests:** The existing test module uses `SessionId` directly (no Session construction needed for ID tests). For tab CRUD tests, you need actual Session instances. Since Session has many fields requiring complex types (PtySender with mpsc, Arc<FairMutex<Term>>), create a helper that constructs a minimal test Session. However, Session requires PtySender (which needs a Poller) and Term (which needs a complex setup). Instead, test the tab management logic through a simpler approach:
       - Create a `#[cfg(test)]` helper method on SessionMux like `new_test(session_ids: &[u64])` that creates a mux with multiple dummy sessions, OR
       - Since SessionMux.close_tab removes from HashMap and returns Session, and we can't easily construct Session in tests, restructure tests to focus on tab index management by testing through the public API of add_tab/close_tab.
       - **Simplest approach:** Add `#[cfg(test)]` module with a helper that creates a minimal Session using test doubles. Use `unsafe` or mock the PtySender/Term only if needed. Alternatively, refactor close_tab to return `Option<(Tab, Session)>` or test only the methods that don't need Session construction (activate, next, prev on a pre-built mux).

    **IMPORTANT:** The Session struct has complex internal types. For testing tab CRUD, the cleanest approach is to add a `#[cfg(test)] pub fn test_session(id: SessionId) -> Session` constructor that creates a Session with minimal/dummy values. This requires glass_terminal test support. If that's too complex, test the index-management aspects (activate, next, prev, tab_count) on a single-session mux created via `SessionMux::new()`, and verify add_tab/close_tab compile correctly with integration tests in Plan 03.

    Actually, the cleanest approach: make the tab management tests not depend on Session at all. Extract tab index logic into methods that only touch `self.tabs` and `self.active_tab`. The HashMap<SessionId, Session> operations in add_tab/close_tab are simple insert/remove. Focus unit tests on the Vec<Tab> + active_tab index logic using a test helper that inserts tabs directly.
  </action>
  <verify>
    <automated>cargo test -p glass_mux -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>Tab has title field. SessionMux has add_tab, close_tab, activate_tab, next_tab, prev_tab, tab_count, active_tab_index, tabs methods. All unit tests pass. cargo test -p glass_mux green.</done>
</task>

</tasks>

<verification>
cargo test -p glass_mux
</verification>

<success_criteria>
- Tab struct has `pub title: String` field
- SessionMux has all 8 new methods (add_tab, close_tab, activate_tab, next_tab, prev_tab, tab_count, active_tab_index, tabs)
- Unit tests cover: add, close, activate, next/prev wraparound, close-middle-adjusts-index
- cargo test -p glass_mux passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-tabs/23-01-SUMMARY.md`
</output>
