---
phase: 05-history-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_history/Cargo.toml
  - crates/glass_history/src/lib.rs
  - crates/glass_history/src/db.rs
  - crates/glass_history/src/search.rs
  - crates/glass_history/src/retention.rs
  - crates/glass_history/src/config.rs
  - Cargo.toml
autonomous: true
requirements: [HIST-01, HIST-03, HIST-04, HIST-05]

must_haves:
  truths:
    - "A HistoryDb can be opened from a file path, creating the SQLite database with WAL mode and schema"
    - "A command record with all metadata fields can be inserted and retrieved"
    - "FTS5 MATCH queries return relevant commands ranked by BM25"
    - "resolve_db_path returns project-local .glass/history.db when .glass/ exists in cwd or ancestor, otherwise global fallback"
    - "Pruning by max age deletes old records; pruning by max size deletes oldest records until under limit"
  artifacts:
    - path: "crates/glass_history/src/db.rs"
      provides: "HistoryDb struct with open(), insert_command(), get_command(), delete_command()"
      exports: ["HistoryDb"]
    - path: "crates/glass_history/src/search.rs"
      provides: "FTS5 search with BM25 ranking"
      exports: ["SearchResult"]
    - path: "crates/glass_history/src/retention.rs"
      provides: "Age-based and size-based pruning"
      exports: ["prune"]
    - path: "crates/glass_history/src/config.rs"
      provides: "HistoryConfig with defaults for max_age, max_size, db path"
      exports: ["HistoryConfig"]
    - path: "crates/glass_history/src/lib.rs"
      provides: "Public API re-exports and resolve_db_path"
      exports: ["HistoryDb", "HistoryConfig", "SearchResult", "CommandRecord", "resolve_db_path"]
  key_links:
    - from: "crates/glass_history/src/db.rs"
      to: "SQLite via rusqlite"
      via: "Connection::open with WAL pragmas and schema creation"
      pattern: "PRAGMA journal_mode = WAL"
    - from: "crates/glass_history/src/db.rs"
      to: "crates/glass_history/src/search.rs"
      via: "HistoryDb exposes search method that delegates to search module"
      pattern: "commands_fts MATCH"
    - from: "crates/glass_history/src/db.rs"
      to: "commands_fts FTS5 table"
      via: "Transactional insert into both commands and commands_fts"
      pattern: "INSERT INTO commands_fts"
---

<objective>
Build the glass_history crate as a fully functional SQLite-backed command history library with FTS5 search, project-aware database paths, and automatic retention policies.

Purpose: This is the data foundation for all v1.1 features -- search overlay, CLI queries, and MCP server all depend on this crate to store and retrieve command history.
Output: A tested library crate that can open/create a database, insert command records, search via FTS5, resolve project vs global database paths, and prune old records.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-database-foundation/05-RESEARCH.md

Key decisions from STATE.md:
- Use content FTS5 tables (not external content) for safety -- meaning standard FTS5 table that stores its own copy, NO content= option
- FTS5 on command text only for v1.1; defer output indexing to v1.2
- rusqlite 0.38.0 with bundled feature already in workspace Cargo.toml
- Store timestamps as INTEGER (Unix epoch seconds)
- Use WAL journal mode
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create glass_history crate with schema, insert, search, path resolution, and retention</name>
  <files>
    crates/glass_history/Cargo.toml,
    crates/glass_history/src/lib.rs,
    crates/glass_history/src/db.rs,
    crates/glass_history/src/search.rs,
    crates/glass_history/src/retention.rs,
    crates/glass_history/src/config.rs,
    Cargo.toml
  </files>
  <behavior>
    - test_insert_and_retrieve: Insert a CommandRecord, retrieve by id, all fields match (command, cwd, exit_code, started_at, finished_at, duration_ms)
    - test_insert_populates_fts: Insert a command with text "cargo build", search for "cargo" returns it
    - test_search_bm25_ranking: Insert 3 commands, one matching query better than others, verify ordering by rank
    - test_search_no_results: Search for nonexistent term returns empty vec
    - test_search_prefix: Search for "car*" matches "cargo build" (FTS5 prefix query)
    - test_resolve_db_path_project: Create tempdir with .glass/ subdirectory, resolve_db_path returns .glass/history.db
    - test_resolve_db_path_ancestor: Create tempdir/sub/sub with .glass/ at root, resolve from sub/sub finds ancestor .glass/history.db
    - test_resolve_db_path_global_fallback: Tempdir with no .glass/, resolve_db_path returns ~/.glass/global-history.db
    - test_prune_by_age: Insert records with old timestamps, prune with max_age=1 day, old records deleted
    - test_prune_by_age_keeps_recent: Recent records survive age pruning
    - test_prune_fts_sync: After pruning, FTS search for pruned command text returns empty
    - test_default_config: HistoryConfig::default() has max_age_days=30, max_size_bytes=1GB
  </behavior>
  <action>
    1. Update crates/glass_history/Cargo.toml to add dependencies: rusqlite (workspace), dirs (workspace), anyhow (workspace), tracing (workspace), serde (workspace). Add [dev-dependencies] tempfile = "3".

    2. Add clap workspace dependency to root Cargo.toml workspace.dependencies section: `clap = { version = "4.5", features = ["derive"] }`. Do NOT add clap to glass_history -- it belongs in the root binary only.

    3. Create config.rs with HistoryConfig struct:
       - max_age_days: u32 (default 30)
       - max_size_bytes: u64 (default 1_073_741_824 = 1GB)
       - Derive serde::Deserialize, Clone, Debug
       - impl Default with the above values

    4. Create db.rs with HistoryDb struct wrapping rusqlite::Connection:
       - pub fn open(path: &Path) -> anyhow::Result<Self>: create_dir_all parent, Connection::open, set WAL pragmas (journal_mode=WAL, synchronous=NORMAL, busy_timeout=5000), call create_schema
       - create_schema: CREATE TABLE IF NOT EXISTS commands (id INTEGER PRIMARY KEY AUTOINCREMENT, command TEXT NOT NULL, cwd TEXT NOT NULL, exit_code INTEGER, started_at INTEGER NOT NULL, finished_at INTEGER NOT NULL, duration_ms INTEGER NOT NULL, created_at INTEGER NOT NULL DEFAULT (unixepoch())). CREATE INDEX on started_at, cwd. CREATE VIRTUAL TABLE IF NOT EXISTS commands_fts USING fts5(command, tokenize='unicode61') -- standard table, NO content= option.
       - pub fn insert_command(&self, record: &CommandRecord) -> anyhow::Result<i64>: Begin transaction, INSERT into commands, get last_insert_rowid, INSERT into commands_fts(rowid, command), commit. Return rowid.
       - pub fn get_command(&self, id: i64) -> anyhow::Result<Option<CommandRecord>>: SELECT by id
       - pub fn delete_command(&self, id: i64) -> anyhow::Result<bool>: DELETE from commands and commands_fts in same transaction, return whether row existed
       - pub fn command_count(&self) -> anyhow::Result<u64>: SELECT COUNT(*)
       - Expose conn() method for search and retention modules to use, or pass Connection reference

    5. Define CommandRecord struct (in lib.rs or db.rs, re-export from lib.rs):
       - id: Option<i64> (None before insert, Some after)
       - command: String
       - cwd: String
       - exit_code: Option<i32>
       - started_at: i64 (Unix epoch seconds)
       - finished_at: i64
       - duration_ms: i64

    6. Create search.rs:
       - SearchResult struct: id, command, cwd, exit_code, started_at, finished_at, duration_ms, rank (f64)
       - pub fn search(conn: &Connection, query: &str, limit: usize) -> anyhow::Result<Vec<SearchResult>>: JOIN commands_fts with commands on rowid, WHERE commands_fts MATCH ?1, ORDER BY rank, LIMIT ?2. Use parameterized query.
       - Add search method to HistoryDb that delegates to this function

    7. Create retention.rs:
       - pub fn prune(conn: &Connection, max_age_days: u32, max_size_bytes: u64) -> anyhow::Result<u64>:
         a. Age pruning: DELETE FROM commands WHERE started_at < cutoff. Also DELETE FROM commands_fts WHERE rowid IN (SELECT id FROM commands WHERE started_at < cutoff) -- BUT must do FTS delete BEFORE commands delete. Use transaction: first SELECT ids to delete, then DELETE from commands_fts WHERE rowid IN (...), then DELETE from commands WHERE id IN (...).
         b. Size pruning: Check page_count * page_size. If over limit, find oldest 10% ids, delete from both tables.
         c. Return total deleted count.
       - Add prune method to HistoryDb that delegates

    8. Create lib.rs with module declarations and public API:
       - mod db; mod search; mod retention; mod config;
       - pub use db::{HistoryDb, CommandRecord}; pub use search::SearchResult; pub use config::HistoryConfig;
       - pub fn resolve_db_path(cwd: &Path) -> PathBuf: Walk up from cwd looking for .glass/ directory. If found, return .glass/history.db. Otherwise return dirs::home_dir()/.glass/global-history.db. Call create_dir_all on the parent.

    IMPORTANT: Use standard FTS5 table (no content= option) per STATE.md decision. This means INSERT into both commands and commands_fts in same transaction, and DELETE from both in same transaction. No triggers needed.

    IMPORTANT: For pruning FTS sync, you must delete from commands_fts BEFORE deleting from commands (since you need the ids). Collect the ids first, delete from FTS by rowid, then delete from commands.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo test -p glass_history -- --nocapture</automated>
  </verify>
  <done>
    - glass_history crate compiles with no warnings
    - All 12+ tests pass: insert/retrieve, FTS5 search (basic, ranking, prefix, no results), path resolution (project, ancestor, global fallback), pruning (age, keeps recent, FTS sync), config defaults
    - HistoryDb, CommandRecord, SearchResult, HistoryConfig, resolve_db_path all exported from lib.rs
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration smoke test -- open database, insert, search, prune end-to-end</name>
  <files>crates/glass_history/src/db.rs</files>
  <action>
    Add an integration-style test (can be in db.rs or a separate tests/ file) that exercises the full lifecycle:

    1. Create a tempdir, open HistoryDb at tempdir/test.db
    2. Insert 5 diverse command records with varying timestamps, cwds, exit codes
    3. Verify command_count() == 5
    4. Search for a specific command term, verify results contain expected record with correct fields
    5. Search with a prefix query ("git*"), verify it matches git-prefixed commands
    6. Insert 3 records with very old timestamps (older than 1 day)
    7. Call prune with max_age_days=1
    8. Verify the old records are gone (command_count decreased by 3)
    9. Verify FTS search for the pruned commands returns empty
    10. Verify the recent commands still exist and are searchable

    Also add a test for resolve_db_path that creates a nested tempdir structure to validate ancestor walking.

    Run `cargo test --workspace` to ensure glass_history doesn't break anything else in the workspace.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Full lifecycle test passes: open -> insert -> search -> prune -> verify
    - Workspace-wide cargo test passes with no regressions
    - glass_history crate is a fully tested, self-contained library ready for consumption by the glass binary
  </done>
</task>

</tasks>

<verification>
- `cargo test -p glass_history` -- all unit and integration tests pass
- `cargo test --workspace` -- no regressions in other crates
- `cargo clippy -p glass_history` -- no warnings
</verification>

<success_criteria>
- glass_history crate builds and passes all tests
- CommandRecord can be inserted with all metadata fields and retrieved correctly
- FTS5 search returns ranked results for MATCH queries including prefix queries
- resolve_db_path correctly walks ancestors for .glass/ and falls back to global
- Pruning removes old records from both commands and commands_fts tables
- No workspace regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-history-database-foundation/05-01-SUMMARY.md`
</output>
