---
phase: 06-output-capture-writer-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/glass_history/src/db.rs
  - crates/glass_history/src/output.rs
  - crates/glass_history/src/config.rs
  - crates/glass_history/src/lib.rs
  - crates/glass_history/Cargo.toml
autonomous: true
requirements: [HIST-02]

must_haves:
  truths:
    - "Schema migration adds output column to existing databases without data loss"
    - "CommandRecord with output inserts and retrieves correctly"
    - "Output exceeding max size is truncated with head+tail split preserving first and last halves"
    - "Binary content is detected and replaced with a placeholder"
    - "ANSI escape sequences are stripped from output before storage"
    - "max_output_capture_kb config field defaults to 50"
  artifacts:
    - path: "crates/glass_history/src/output.rs"
      provides: "Output processing: ANSI stripping, binary detection, head+tail truncation"
      exports: ["process_output", "truncate_head_tail", "is_binary", "strip_ansi"]
    - path: "crates/glass_history/src/db.rs"
      provides: "Schema migration v0->v1, CommandRecord with output field, updated insert/get"
      contains: "output: Option<String>"
    - path: "crates/glass_history/src/config.rs"
      provides: "max_output_capture_kb config field"
      contains: "max_output_capture_kb"
  key_links:
    - from: "crates/glass_history/src/db.rs"
      to: "PRAGMA user_version"
      via: "migrate() called from open()"
      pattern: "user_version"
    - from: "crates/glass_history/src/output.rs"
      to: "strip-ansi-escapes crate"
      via: "strip_ansi function"
      pattern: "strip_ansi"
---

<objective>
Add output storage support to the glass_history crate: schema migration for the output column, output processing pipeline (ANSI stripping, binary detection, head+tail truncation), and config extension.

Purpose: Provides the data layer that Plan 02 will wire into the PTY capture pipeline. All processing logic is pure functions, ideal for TDD.
Output: Updated glass_history crate with output column, processing functions, and tests.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-capture-writer-integration/06-CONTEXT.md
@.planning/phases/06-output-capture-writer-integration/06-RESEARCH.md

<interfaces>
<!-- Existing glass_history interfaces this plan modifies -->

From crates/glass_history/src/db.rs:
```rust
pub struct CommandRecord {
    pub id: Option<i64>,
    pub command: String,
    pub cwd: String,
    pub exit_code: Option<i32>,
    pub started_at: i64,
    pub finished_at: i64,
    pub duration_ms: i64,
    // Plan adds: pub output: Option<String>,
}

pub struct HistoryDb { conn: Connection }
impl HistoryDb {
    pub fn open(path: &Path) -> Result<Self>;
    pub fn insert_command(&self, record: &CommandRecord) -> Result<i64>;
    pub fn get_command(&self, id: i64) -> Result<Option<CommandRecord>>;
    // ... other methods unchanged
}
```

From crates/glass_history/src/config.rs:
```rust
pub struct HistoryConfig {
    pub max_age_days: u32,
    pub max_size_bytes: u64,
    // Plan adds: pub max_output_capture_kb: u32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Output processing module with TDD</name>
  <files>crates/glass_history/src/output.rs, crates/glass_history/src/lib.rs, crates/glass_history/Cargo.toml</files>
  <behavior>
    - strip_ansi("\x1b[31mhello\x1b[0m") -> "hello"
    - strip_ansi("no escapes") -> "no escapes"
    - strip_ansi("\x1b[38;2;255;0;0mcolor\x1b[0m") -> "color" (24-bit color)
    - is_binary(b"hello world\n") -> false
    - is_binary(bytes with >30% non-printable excluding \n\r\t) -> true
    - is_binary(b"") -> false
    - truncate_head_tail("short", 1000) -> "short" (under limit, no truncation)
    - truncate_head_tail(100-byte string, 50) -> first ~25 bytes + "[...truncated N bytes...]" + last ~25 bytes
    - truncate_head_tail preserves UTF-8 boundaries (no panic on multi-byte chars)
    - process_output(raw_bytes, max_kb=50) -> ProcessedOutput with text for normal output
    - process_output(binary_bytes, max_kb=50) -> ProcessedOutput with "[binary output: N bytes]" placeholder
    - process_output(large_output, max_kb=1) -> ProcessedOutput with truncated text
    - process_output for alt-screen data (None input) -> None
  </behavior>
  <action>
    RED: Create `crates/glass_history/src/output.rs` with tests first. Write tests for:
    1. `strip_ansi` -- uses `strip-ansi-escapes` crate to strip all ANSI sequences from bytes, returns String
    2. `is_binary` -- samples first 8KB, returns true if >30% non-printable (excluding \n, \r, \t)
    3. `truncate_head_tail` -- takes &str and max_bytes, keeps first half + "[...truncated N bytes...]" + last half. Uses `str::floor_char_boundary` / `str::ceil_char_boundary` for safe UTF-8 splits
    4. `process_output` -- main entry point: takes `Option<Vec<u8>>` raw bytes and max_kb, returns `Option<String>`. Pipeline: None->None, strip ANSI, check binary (if binary return placeholder), convert to String (lossy), truncate if needed

    Add `strip-ansi-escapes` to `crates/glass_history/Cargo.toml`.
    Add `pub mod output;` to `lib.rs`.

    GREEN: Implement each function minimally to pass tests.

    REFACTOR: Clean up, ensure all edge cases covered.

    Per user decision: head+tail split with "[...truncated N bytes...]" marker. Binary detection stores "[binary output: N bytes]" placeholder. ANSI stripped before storage.
  </action>
  <verify>
    <automated>cargo test -p glass_history output</automated>
  </verify>
  <done>All output processing tests pass. strip_ansi, is_binary, truncate_head_tail, and process_output functions exported and working.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema migration and CommandRecord output field with TDD</name>
  <files>crates/glass_history/src/db.rs, crates/glass_history/src/config.rs</files>
  <behavior>
    - Opening a fresh database creates commands table with output column and sets user_version=1
    - Opening a Phase 5 database (user_version=0, no output column) migrates: adds output column, sets user_version=1
    - Existing records survive migration with output=NULL
    - insert_command with output=Some("hello") stores and retrieves correctly
    - insert_command with output=None stores NULL and retrieves as None
    - get_command returns the output field
    - HistoryConfig default max_output_capture_kb is 50
    - Config TOML with max_output_capture_kb = 100 parses correctly
  </behavior>
  <action>
    RED: Add tests to db.rs:
    1. `test_migration_v0_to_v1` -- create a v0 database manually (CREATE TABLE without output column, PRAGMA user_version=0), then open via HistoryDb::open, verify output column exists
    2. `test_insert_with_output` -- insert CommandRecord with output=Some("stdout content"), retrieve, verify output matches
    3. `test_insert_without_output` -- insert CommandRecord with output=None, retrieve, verify output is None
    4. `test_existing_records_survive_migration` -- insert record into v0 DB, migrate, verify record still accessible with output=None

    Add test to config.rs:
    5. `test_max_output_capture_kb_default` -- verify default is 50
    6. `test_max_output_capture_kb_from_toml` -- parse TOML with the field

    GREEN:
    1. Add `output: Option<String>` to `CommandRecord`
    2. Add `migrate()` function using `PRAGMA user_version`: if version < 1, run `ALTER TABLE commands ADD COLUMN output TEXT; PRAGMA user_version = 1;`
    3. Call `migrate()` from `open()` after `create_schema()`
    4. Update `create_schema()` to include output column in CREATE TABLE and set user_version=1
    5. Update `insert_command()` to include output in INSERT (as ?7)
    6. Update `get_command()` to SELECT and map the output column
    7. Add `max_output_capture_kb: u32` to `HistoryConfig` with default 50
    8. Update all existing tests that construct CommandRecord to include `output: None`

    REFACTOR: Ensure migration is idempotent (already at v1 = no-op).
  </action>
  <verify>
    <automated>cargo test -p glass_history</automated>
  </verify>
  <done>Schema migration works for fresh and existing databases. CommandRecord stores and retrieves output. All existing tests still pass with the new field. HistoryConfig has max_output_capture_kb defaulting to 50.</done>
</task>

</tasks>

<verification>
cargo test -p glass_history -- all tests pass including new output processing and migration tests
cargo test --workspace -- no regressions
</verification>

<success_criteria>
- glass_history crate has output.rs module with strip_ansi, is_binary, truncate_head_tail, process_output
- CommandRecord has output: Option<String> field
- commands table has output TEXT column (fresh and migrated)
- PRAGMA user_version migration pattern established
- HistoryConfig has max_output_capture_kb: u32 defaulting to 50
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-capture-writer-integration/06-01-SUMMARY.md`
</output>
