---
phase: 30-documentation-distribution
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - .github/workflows/docs.yml
  - README.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "GitHub Actions workflow builds mdBook and deploys to GitHub Pages on push to main"
    - "README contains installation instructions for all three platforms"
    - "README contains feature overview with key capabilities"
    - "README contains CI badge and license badge"
    - "README links to the full documentation site"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "GitHub Pages deployment workflow"
      contains: "actions/deploy-pages"
    - path: "README.md"
      provides: "Project README with install instructions and features"
      min_lines: 50
      contains: "Installation"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "docs/"
      via: "mdbook build docs"
      pattern: "mdbook build"
    - from: "README.md"
      to: "docs site"
      via: "documentation link"
      pattern: "Documentation"
---

<objective>
Create the GitHub Pages deployment workflow for the docs site and write the project README with screenshots placeholders, installation instructions, feature overview, and badges.

Purpose: Make documentation publicly accessible and give the GitHub repo a professional landing page.
Output: CI workflow for docs deployment and a complete README.md.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-documentation-distribution/30-RESEARCH.md
@.planning/phases/30-documentation-distribution/30-01-SUMMARY.md
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Pages deployment workflow</name>
  <files>.github/workflows/docs.yml</files>
  <action>
Create `.github/workflows/docs.yml` following the official GitHub Actions Pages workflow pattern from the research.

The workflow should:
1. Trigger on push to `main` branch and `workflow_dispatch` (manual trigger)
2. Set permissions: contents read, pages write, id-token write
3. Use concurrency group `pages` with `cancel-in-progress: false`
4. Job `build`:
   - runs-on: ubuntu-latest
   - Checkout repo
   - Install mdBook using `peaceiris/actions-mdbook@v2` with `mdbook-version: 'latest'`
   - Run `mdbook build docs`
   - Upload artifact with `actions/upload-pages-artifact@v3` pointing to `docs/book`
5. Job `deploy`:
   - needs: build
   - runs-on: ubuntu-latest
   - environment: name `github-pages`, url from deployment output
   - Deploy with `actions/deploy-pages@v4`

Add a YAML comment at the top noting that GitHub Pages must be configured in repo Settings to use "GitHub Actions" as the source (not "Deploy from a branch").

Reference the existing release.yml for style consistency (action versions, formatting).
  </action>
  <verify>
    <automated>cat .github/workflows/docs.yml | head -5</automated>
  </verify>
  <done>docs.yml exists with correct trigger (main branch push), mdBook build step, and deploy-pages action</done>
</task>

<task type="auto">
  <name>Task 2: Write project README</name>
  <files>README.md</files>
  <action>
Create `README.md` at the project root. Use `<GITHUB_USER>` as placeholder for the GitHub username (no remote configured yet).

Structure:
1. **Title and badges:**
   - `# Glass`
   - CI badge: `![CI](https://github.com/<GITHUB_USER>/glass/actions/workflows/ci.yml/badge.svg)`
   - License badge: `![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)`
   - Leave a comment `<!-- TODO: Add screenshot -->` where a screenshot/GIF would go

2. **Tagline:** "GPU-accelerated terminal emulator with command structure awareness."

3. **One-paragraph description:** Glass treats terminal output as structured data. Every command produces a visually distinct block showing exit code, duration, and working directory. Built with Rust and wgpu for GPU-accelerated rendering.

4. **Features section** (bulleted list):
   - Command blocks with exit codes, duration, and working directory
   - Ctrl+Shift+F full-text search across terminal history (FTS5/SQLite)
   - Ctrl+Shift+Z file undo -- restore files modified by commands
   - Pipeline inspection with intermediate stage output
   - Tabs (Ctrl+Shift+T) and split panes (Ctrl+Shift+D)
   - MCP server for AI assistant integration
   - Hot-reloadable configuration
   - Auto-update notifications
   - Cross-platform: Windows, macOS, Linux

5. **Installation section** with subsections for each platform:
   - **Windows:** MSI from Releases + `winget install Glass.Terminal`
   - **macOS:** DMG from Releases + `brew install <GITHUB_USER>/glass/glass`, include Gatekeeper note
   - **Linux:** .deb from Releases + `sudo dpkg -i` command

6. **Quick Start** (3-4 lines):
   - Launch Glass, it detects your shell automatically
   - Edit `~/.glass/config.toml` to customize (changes apply live)
   - See documentation link for full configuration reference

7. **Documentation:** Link to `https://<GITHUB_USER>.github.io/glass/` (the GitHub Pages site)

8. **Building from source:**
   - Prerequisites: Rust toolchain, platform-specific deps (reference CI workflow)
   - `cargo build --release`
   - Binary at `target/release/glass`

9. **License:** MIT
  </action>
  <verify>
    <automated>wc -l README.md</automated>
  </verify>
  <done>README.md exists with 50+ lines, contains Installation section with all 3 platforms, Features list with 8+ items, CI and license badges, documentation link, and build instructions</done>
</task>

</tasks>

<verification>
1. docs.yml follows the official GitHub Actions Pages pattern
2. README.md contains all required sections: badges, features, installation (3 platforms), documentation link
3. No hardcoded GitHub URLs -- uses <GITHUB_USER> placeholder consistently
</verification>

<success_criteria>
- .github/workflows/docs.yml is valid YAML with mdBook build and Pages deploy jobs
- README.md has installation instructions for Windows (MSI + winget), macOS (DMG + brew), Linux (deb)
- README.md has feature overview covering all major Glass capabilities
- README.md has CI and license badges
- README.md links to the documentation site
</success_criteria>

<output>
After completion, create `.planning/phases/30-documentation-distribution/30-02-SUMMARY.md`
</output>
