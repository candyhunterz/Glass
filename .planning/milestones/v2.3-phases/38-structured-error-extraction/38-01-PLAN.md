---
phase: 38-structured-error-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_errors/Cargo.toml
  - crates/glass_errors/src/lib.rs
  - crates/glass_errors/src/rust_json.rs
  - crates/glass_errors/src/rust_human.rs
  - crates/glass_errors/src/generic.rs
  - crates/glass_errors/src/detect.rs
autonomous: true
requirements: [ERR-02, ERR-03, ERR-04]

must_haves:
  truths:
    - "Rust JSON parser extracts structured errors from cargo --message-format=json output"
    - "Rust JSON parser extracts structured errors from raw rustc --error-format=json output"
    - "Rust human parser extracts errors from error[E0xxx] + --> file:line:col format"
    - "Generic parser extracts errors from file:line:col: severity: message format"
    - "Generic parser handles Windows paths with drive letters (C:\\...)"
    - "Auto-detection selects Rust JSON parser when command hint contains cargo/rustc and output has JSON"
    - "Auto-detection selects Rust human parser when output contains error[E patterns"
    - "Auto-detection falls back to generic parser for unknown output"
  artifacts:
    - path: "crates/glass_errors/Cargo.toml"
      provides: "Crate manifest with regex, serde, serde_json deps"
    - path: "crates/glass_errors/src/lib.rs"
      provides: "Public API: extract_errors(), StructuredError, Severity"
      exports: ["extract_errors", "StructuredError", "Severity"]
    - path: "crates/glass_errors/src/rust_json.rs"
      provides: "Rust JSON diagnostic parser (cargo + raw rustc)"
    - path: "crates/glass_errors/src/rust_human.rs"
      provides: "Rust human-readable error parser"
    - path: "crates/glass_errors/src/generic.rs"
      provides: "Generic file:line:col parser"
    - path: "crates/glass_errors/src/detect.rs"
      provides: "Auto-detection logic"
  key_links:
    - from: "crates/glass_errors/src/lib.rs"
      to: "crates/glass_errors/src/detect.rs"
      via: "extract_errors calls detect_parser"
      pattern: "detect_parser"
    - from: "crates/glass_errors/src/detect.rs"
      to: "crates/glass_errors/src/rust_json.rs"
      via: "ParserKind::RustJson dispatches to parse_rust_json"
      pattern: "parse_rust_json"
---

<objective>
Create the glass_errors pure library crate with all error parsers and auto-detection logic.

Purpose: Provide a reusable, dependency-free-from-Glass library that transforms raw command output into structured error records (file, line, column, message, severity, code).
Output: A fully tested `glass_errors` crate with Rust JSON, Rust human-readable, and generic parsers plus auto-detection.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-structured-error-extraction/38-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create glass_errors crate with types, detection, and generic parser</name>
  <files>crates/glass_errors/Cargo.toml, crates/glass_errors/src/lib.rs, crates/glass_errors/src/detect.rs, crates/glass_errors/src/generic.rs</files>
  <behavior>
    - extract_errors("src/main.c:10:5: error: undeclared", None) returns 1 StructuredError with file="src/main.c", line=10, column=Some(5), severity=Error, message="undeclared"
    - extract_errors("C:\\Users\\foo\\main.rs:10:5: warning: unused", None) returns StructuredError with correct Windows path
    - extract_errors with file:line: severity: message (no column) returns column=None
    - extract_errors with file:line:col: message (no explicit severity) returns severity=Error as default
    - detect_parser with command_hint="cargo build" and JSON output returns RustJson
    - detect_parser with command_hint="cargo test" and non-JSON output returns RustHuman
    - detect_parser with command_hint=None and output containing "error[E" returns RustHuman
    - detect_parser with command_hint=None and generic output returns Generic
    - detect_parser with output containing "$message_type":"diagnostic" returns RustJson
  </behavior>
  <action>
    1. Create `crates/glass_errors/Cargo.toml` with name "glass_errors", edition "2021", dependencies: regex = "1" (workspace if available, else version), serde = { workspace = true }, serde_json = "1.0". Add the crate to workspace members (already auto-included via `crates/*` glob -- verify).

    2. Create `crates/glass_errors/src/lib.rs` with:
       - `StructuredError` struct: file (String), line (u32), column (Option<u32>), severity (Severity), message (String), code (Option<String>). Derive Debug, Clone, Serialize.
       - `Severity` enum: Error, Warning, Note, Help. Derive Debug, Clone, Serialize, PartialEq. Use `#[serde(rename_all = "lowercase")]`.
       - Public modules: `mod rust_json; mod rust_human; mod generic; mod detect;`
       - Public entry point: `pub fn extract_errors(output: &str, command_hint: Option<&str>) -> Vec<StructuredError>` that calls `detect::detect_parser()` then dispatches to the appropriate parser's `parse()` function.
       - `ParserKind` enum: RustJson, RustHuman, Generic. Keep internal (pub(crate)).

    3. Create `crates/glass_errors/src/detect.rs` with:
       - `pub(crate) fn detect_parser(output: &str, command_hint: Option<&str>) -> ParserKind`
       - Priority: (a) command hint containing "cargo" or "rustc" -> check if output has JSON lines -> RustJson, else RustHuman. (b) Content sniffing: `"$message_type":"diagnostic"` or `"reason":"compiler-message"` -> RustJson. `error[E` or `warning[` -> RustHuman. (c) Fallback: Generic.
       - Tests in `#[cfg(test)] mod tests` covering all detection paths.

    4. Create `crates/glass_errors/src/generic.rs` with:
       - `pub(crate) fn parse_generic(output: &str) -> Vec<StructuredError>`
       - Use `std::sync::OnceLock` for compiled regexes (NOT lazy_static -- use OnceLock per project conventions).
       - Three regex tiers using `Regex::new()`:
         (a) `^([A-Za-z]:\\[^:]+|[^:\s][^:]*):(\d+):(\d+):\s*(?i)(error|warning|note|info|hint):\s*(.+)$` -- file:line:col: severity: message
         (b) `^([A-Za-z]:\\[^:]+|[^:\s][^:]*):(\d+):\s*(?i)(error|warning|note|info|hint):\s*(.+)$` -- file:line: severity: message (no column)
         (c) `^([A-Za-z]:\\[^:]+|[^:\s][^:]*):(\d+):(\d+):\s*(.+)$` -- file:line:col: message (assume error)
       - Map severity strings to Severity enum (case-insensitive). "info" and "hint" map to Note and Help respectively.
       - Process line-by-line, try regex (a) first, then (b), then (c). First match wins per line.
       - Tests: standard format, Windows paths, no-column format, no-severity format, mixed output with non-error lines.

    Verify cargo builds and all tests pass before completing.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo test --package glass_errors -- detect generic 2>&1 | tail -20</automated>
  </verify>
  <done>glass_errors crate exists with StructuredError/Severity types, generic parser handles file:line:col patterns including Windows paths, auto-detection selects correct parser kind. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Rust JSON and human-readable parsers</name>
  <files>crates/glass_errors/src/rust_json.rs, crates/glass_errors/src/rust_human.rs</files>
  <behavior>
    - Rust JSON parser: cargo wrapper format {"reason":"compiler-message","message":{diagnostic}} extracts file, line, column, severity, message, code
    - Rust JSON parser: raw rustc format {"$message_type":"diagnostic",...} extracts errors
    - Rust JSON parser: skips non-diagnostic cargo lines (compiler-artifact, build-finished)
    - Rust JSON parser: skips diagnostics with empty spans (e.g. "aborting due to N errors")
    - Rust JSON parser: extracts primary span (is_primary=true) when multiple spans exist
    - Rust JSON parser: maps "error" level to Error, "warning" to Warning, "note" to Note, "help" to Help
    - Rust JSON parser: extracts error code from code.code field (e.g. "E0308")
    - Rust human parser: "error[E0308]: mismatched types\n --> src/main.rs:10:5" extracts file, line, col, message, code
    - Rust human parser: "warning: unused variable\n --> src/lib.rs:3:9" extracts warning with no code
    - Rust human parser: handles multi-error output, extracting each error separately
    - End-to-end: extract_errors with Rust JSON output and command_hint="cargo build" returns correct errors
    - End-to-end: extract_errors with human-readable output and no hint returns correct errors via content sniffing
  </behavior>
  <action>
    1. Create `crates/glass_errors/src/rust_json.rs` with:
       - Internal structs (Deserialize only, not public): `CargoMessage { reason: String, message: Option<RustDiagnostic> }`, `RustDiagnostic { message: String, level: String, code: Option<DiagCode>, spans: Vec<DiagSpan> }`, `DiagCode { code: String }`, `DiagSpan { file_name: String, line_start: u32, column_start: u32, is_primary: bool }`.
       - Use `#[serde(default)]` on optional/vec fields for robustness.
       - `pub(crate) fn parse_rust_json(output: &str) -> Vec<StructuredError>`
       - Process line-by-line. Skip lines not starting with '{'.
       - Try cargo wrapper first (`serde_json::from_str::<CargoMessage>`). If `reason == "compiler-message"` and message is Some, extract diagnostic. Otherwise skip.
       - If cargo parse fails, try raw rustc (`serde_json::from_str::<RustDiagnostic>`).
       - `collect_diagnostic(diag, &mut errors)`: find primary span (`is_primary == true`), map level to Severity, create StructuredError. Skip diagnostics with no primary span.
       - Tests with realistic cargo and rustc JSON output (copy examples from research).

    2. Create `crates/glass_errors/src/rust_human.rs` with:
       - `pub(crate) fn parse_rust_human(output: &str) -> Vec<StructuredError>`
       - Use OnceLock for two regexes:
         (a) Header: `^(error|warning|note|help)(\[([A-Z]\d+)\])?:\s*(.+)$` -- captures severity, optional code, message
         (b) Span: `^\s*-->\s*(.+):(\d+):(\d+)$` -- captures file, line, column
       - Iterate lines. When header matches, store severity+code+message. When next `-->` span matches, emit StructuredError combining stored header with span location. Reset state.
       - Handle case where header has no following `-->` line (skip, don't emit).
       - Tests with realistic rustc human output including multiple errors, warnings, notes.

    Run the full glass_errors test suite to verify all parsers work together including auto-detection integration.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo test --package glass_errors 2>&1 | tail -20</automated>
  </verify>
  <done>Rust JSON parser handles both cargo and raw rustc JSON formats. Rust human parser handles error[Exxx] with --> spans. Auto-detection correctly routes to all three parsers. All glass_errors tests pass. `cargo clippy --package glass_errors -- -D warnings` clean.</done>
</task>

</tasks>

<verification>
```bash
export PATH="/c/Users/nkngu/.cargo/bin:$PATH"
cargo test --package glass_errors
cargo clippy --package glass_errors -- -D warnings
```
</verification>

<success_criteria>
- glass_errors crate compiles with no warnings
- All unit tests pass for generic, rust_json, rust_human, and detect modules
- extract_errors() correctly auto-selects parser and returns structured errors for all three formats
- Windows paths handled correctly in generic parser
- Cargo wrapper JSON and raw rustc JSON both parsed correctly
</success_criteria>

<output>
After completion, create `.planning/phases/38-structured-error-extraction/38-01-SUMMARY.md`
</output>
