---
phase: 10-content-store-db-schema
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/glass_snapshot/Cargo.toml
  - crates/glass_snapshot/src/types.rs
  - crates/glass_snapshot/src/blob_store.rs
  - crates/glass_snapshot/src/db.rs
  - crates/glass_snapshot/src/lib.rs
autonomous: true
requirements: [SNAP-02, SNAP-06]

must_haves:
  truths:
    - "Storing the same file twice produces only one blob on disk"
    - "A stored blob can be read back with byte-identical content"
    - "Snapshot metadata persists across DB close and reopen"
    - "Snapshot records link to a command_id integer"
    - "Deleting a snapshot cascades to its snapshot_files rows"
  artifacts:
    - path: "crates/glass_snapshot/src/blob_store.rs"
      provides: "Content-addressed file storage with BLAKE3 hashing and deduplication"
      exports: ["BlobStore"]
    - path: "crates/glass_snapshot/src/db.rs"
      provides: "SQLite snapshots.db schema, migrations, CRUD operations"
      exports: ["SnapshotDb"]
    - path: "crates/glass_snapshot/src/types.rs"
      provides: "Shared types for snapshot records"
      exports: ["SnapshotRecord", "SnapshotFileRecord"]
    - path: "crates/glass_snapshot/src/lib.rs"
      provides: "Public API, re-exports, SnapshotStore coordinator"
      exports: ["SnapshotStore", "BlobStore", "SnapshotDb"]
  key_links:
    - from: "crates/glass_snapshot/src/lib.rs"
      to: "crates/glass_snapshot/src/blob_store.rs"
      via: "SnapshotStore delegates file storage to BlobStore"
      pattern: "self\\.blobs\\.store_file"
    - from: "crates/glass_snapshot/src/lib.rs"
      to: "crates/glass_snapshot/src/db.rs"
      via: "SnapshotStore delegates metadata to SnapshotDb"
      pattern: "self\\.db\\.(create_snapshot|insert_snapshot_file)"
    - from: "crates/glass_snapshot/src/db.rs"
      to: "snapshots.db"
      via: "SQLite with WAL mode, foreign_keys ON"
      pattern: "PRAGMA foreign_keys = ON"
---

<objective>
Build the glass_snapshot crate with content-addressed blob storage (BLAKE3) and snapshot metadata database (SQLite).

Purpose: This is the data foundation for command-level undo. All downstream phases (11-14) depend on this crate to store file contents and track snapshot metadata.
Output: Working glass_snapshot crate with BlobStore, SnapshotDb, SnapshotStore, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-content-store-db-schema/10-RESEARCH.md
@.planning/phases/10-content-store-db-schema/10-VALIDATION.md

<interfaces>
<!-- Existing glass_history pattern to follow for consistency -->

From crates/glass_history/src/db.rs (schema pattern):
```rust
// PRAGMA user_version migration pattern
// PRAGMA journal_mode = WAL
// PRAGMA synchronous = NORMAL
// PRAGMA busy_timeout = 5000
// pub fn open(path: &Path) -> anyhow::Result<Self>
// create_dir_all for parent directory
```

From crates/glass_history/src/lib.rs (re-export pattern):
```rust
pub use db::HistoryDb;
pub use config::RetentionConfig;
// etc -- re-export public types from lib.rs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BlobStore with content-addressed storage and deduplication</name>
  <files>Cargo.toml, crates/glass_snapshot/Cargo.toml, crates/glass_snapshot/src/types.rs, crates/glass_snapshot/src/blob_store.rs, crates/glass_snapshot/src/lib.rs</files>
  <behavior>
    - test_hash_correctness: blake3::hash of known content "hello world" produces expected hex string
    - test_store_and_read: store a file via BlobStore::store_file, read it back via read_blob, content is byte-identical
    - test_dedup: store the same file twice, count blobs on disk, only one .blob file exists
    - test_blob_exists: after storing, blob_exists returns true; for unknown hash returns false
    - test_delete_blob: after storing and deleting, blob_exists returns false, read_blob fails
    - test_shard_directory: blob is stored under {blob_dir}/{hash[0:2]}/{hash}.blob
  </behavior>
  <action>
1. Add `blake3 = "1.8.3"` to workspace dependencies in root Cargo.toml.
2. Update crates/glass_snapshot/Cargo.toml:
   - [dependencies]: blake3 (workspace), rusqlite (workspace), anyhow (workspace), tracing (workspace), dirs (workspace)
   - [dev-dependencies]: tempfile = "3"
3. Create types.rs with SnapshotRecord { id, command_id, cwd, created_at } and SnapshotFileRecord { id, snapshot_id, file_path, blob_hash (Option), file_size (Option), source }.
4. Create blob_store.rs with BlobStore struct following the research pattern exactly:
   - new(glass_dir: &Path) -> Self (blob_dir = glass_dir.join("blobs"))
   - store_file(source_path: &Path) -> Result<(String, u64)> -- read file, blake3::hash, hex, shard dir, skip write if exists
   - read_blob(hash: &str) -> Result<Vec<u8>>
   - blob_exists(hash: &str) -> bool
   - delete_blob(hash: &str) -> Result<bool>
   All methods use 2-char hex prefix directory sharding: {blob_dir}/{hash[..2]}/{hash}.blob
5. Update lib.rs with module declarations (mod types; mod blob_store; mod db;) and re-exports. Leave db module as empty placeholder (pub mod db;) with just a comment -- it is implemented in Task 2.
6. Follow TDD: write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cargo test -p glass_snapshot -- blob_store::tests -x</automated>
  </verify>
  <done>BlobStore stores files by BLAKE3 hash with 2-char sharding, deduplicates identical content (one blob on disk), reads back byte-identical content. All 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SnapshotDb with schema, migrations, and CRUD + SnapshotStore coordinator</name>
  <files>crates/glass_snapshot/src/db.rs, crates/glass_snapshot/src/lib.rs</files>
  <behavior>
    - test_schema_creation: open DB, verify snapshots and snapshot_files tables exist with correct columns
    - test_persistence: insert snapshot, close DB, reopen at same path, query returns the same record
    - test_command_id_link: create snapshot with command_id=42, query by command_id, returns correct snapshot
    - test_cascade_delete: create snapshot with files, delete snapshot, snapshot_files rows are gone (foreign_keys ON)
    - test_insert_snapshot_file_null_hash: insert file with None hash (file did not exist), query returns NULL hash
    - test_snapshot_store_integration: SnapshotStore.open, create_snapshot, store_file, verify blob on disk AND metadata in DB
  </behavior>
  <action>
1. Create db.rs with SnapshotDb struct following glass_history pattern exactly:
   - open(path: &Path) -> Result<Self>: create_dir_all parent, Connection::open, PRAGMA journal_mode=WAL, synchronous=NORMAL, busy_timeout=5000, foreign_keys=ON, create_schema, migrate
   - create_schema: CREATE TABLE IF NOT EXISTS snapshots (id INTEGER PRIMARY KEY AUTOINCREMENT, command_id INTEGER NOT NULL, cwd TEXT NOT NULL, created_at INTEGER NOT NULL DEFAULT (unixepoch())); CREATE INDEX idx_snapshots_command ON snapshots(command_id); CREATE TABLE IF NOT EXISTS snapshot_files (id INTEGER PRIMARY KEY AUTOINCREMENT, snapshot_id INTEGER NOT NULL REFERENCES snapshots(id) ON DELETE CASCADE, file_path TEXT NOT NULL, blob_hash TEXT, file_size INTEGER, source TEXT NOT NULL DEFAULT 'parser'); CREATE INDEX idx_sf_snapshot ON snapshot_files(snapshot_id); CREATE INDEX idx_sf_hash ON snapshot_files(blob_hash);
   - migrate: PRAGMA user_version check, set to 1 if < 1
   - create_snapshot(command_id: i64, cwd: &str) -> Result<i64>: INSERT INTO snapshots, return last_insert_rowid
   - insert_snapshot_file(snapshot_id: i64, file_path: &Path, blob_hash: Option<&str>, file_size: Option<u64>, source: &str) -> Result<()>
   - get_snapshot(id: i64) -> Result<Option<SnapshotRecord>>
   - get_snapshot_files(snapshot_id: i64) -> Result<Vec<SnapshotFileRecord>>
   - get_snapshots_by_command(command_id: i64) -> Result<Vec<SnapshotRecord>>
   - delete_snapshot(id: i64) -> Result<bool>
   - update_command_id(snapshot_id: i64, command_id: i64) -> Result<()>
2. Update lib.rs: add SnapshotStore struct combining BlobStore + SnapshotDb (following research pattern). Methods: open(glass_dir), create_snapshot, store_file (handles non-existent files with NULL hash, skips symlinks), update_command_id. Also add resolve_glass_dir and resolve_snapshot_db_path functions (ancestor-walk pattern from glass_history).
3. Re-export all public types: SnapshotStore, BlobStore, SnapshotDb, SnapshotRecord, SnapshotFileRecord.
4. CRITICAL: Do NOT import anything from glass_history. The command_id is just an i64.
5. Follow TDD: write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cargo test -p glass_snapshot -x</automated>
  </verify>
  <done>SnapshotDb creates schema with correct tables/indexes, persists across reopen, links snapshots to command_id, cascading deletes work, NULL hash supported for absent files. SnapshotStore coordinates BlobStore + SnapshotDb end-to-end. All tests pass. `cargo test --workspace` passes (no regressions).</done>
</task>

</tasks>

<verification>
1. `cargo test -p glass_snapshot` -- all unit tests pass
2. `cargo test --workspace` -- no regressions in existing crates
3. `cargo build --workspace` -- clean build with no warnings
</verification>

<success_criteria>
- glass_snapshot crate compiles with blake3, rusqlite, anyhow, tracing, dirs dependencies
- BlobStore stores and deduplicates files using BLAKE3 content-addressed hashing
- SnapshotDb persists snapshot metadata in SQLite with WAL mode and foreign key cascades
- SnapshotStore provides high-level API coordinating both subsystems
- All tests pass, workspace builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-store-db-schema/10-01-SUMMARY.md`
</output>
