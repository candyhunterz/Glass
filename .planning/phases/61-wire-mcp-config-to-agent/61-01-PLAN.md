---
phase: 61-wire-mcp-config-to-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_core/src/agent_runtime.rs
  - src/main.rs
autonomous: true
requirements: [AGTR-03, SOIM-01, SOIM-02, SOIM-03]

must_haves:
  truths:
    - "Agent subprocess receives --mcp-config pointing to a valid JSON file listing the Glass MCP server"
    - "build_agent_command_args never emits a dangling --mcp-config flag when path is empty"
    - "flush_collapsed() is called before every agent_runtime = None so last collapsed event is not dropped"
    - "Default allowed_tools includes glass_query_trend and glass_query_drill"
  artifacts:
    - path: "crates/glass_core/src/agent_runtime.rs"
      provides: "Conditional --mcp-config emission, updated default allowed_tools"
      contains: "if !mcp_config_path.is_empty()"
    - path: "src/main.rs"
      provides: "MCP config JSON generation, flush_collapsed at shutdown sites"
      contains: "agent-mcp.json"
  key_links:
    - from: "src/main.rs"
      to: "crates/glass_core/src/agent_runtime.rs"
      via: "build_agent_command_args call with valid mcp_config_path"
      pattern: "build_agent_command_args.*mcp_path_str"
    - from: "src/main.rs"
      to: "crates/glass_core/src/activity_stream.rs"
      via: "flush_collapsed() call before agent_runtime = None"
      pattern: "flush_collapsed"
---

<objective>
Fix MCP config wiring so the agent subprocess can discover and invoke Glass MCP tools, call flush_collapsed on shutdown, and update default allowed_tools.

Purpose: Closes the dangling --mcp-config flag bug and the missing flush_collapsed call identified in the v3.0 milestone audit. Without this fix, the agent subprocess cannot call glass_query/glass_query_trend/glass_query_drill.
Output: Working MCP config JSON generation, defensive guard in build_agent_command_args, flush_collapsed at all shutdown sites, updated allowed_tools default.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-wire-mcp-config-to-agent/61-RESEARCH.md
@.planning/phases/61-wire-mcp-config-to-agent/61-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/glass_core/src/agent_runtime.rs:
```rust
pub struct AgentRuntimeConfig {
    pub mode: AgentMode,
    pub max_budget_usd: f64,
    pub cooldown_secs: u64,
    pub allowed_tools: String,
}

pub fn build_agent_command_args(
    config: &AgentRuntimeConfig,
    prompt_path: &str,
    mcp_config_path: &str,
) -> Vec<String>;
```

From crates/glass_core/src/activity_stream.rs:
```rust
impl ActivityFilter {
    pub fn flush_collapsed(&mut self) -> Option<ActivityEvent>;
}
```

From src/main.rs (Processor fields):
```rust
activity_stream_tx: Option<std::sync::mpsc::SyncSender<ActivityEvent>>,
activity_filter: glass_core::activity_stream::ActivityFilter,
agent_runtime: Option<AgentRuntime>,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix build_agent_command_args and update default allowed_tools</name>
  <files>crates/glass_core/src/agent_runtime.rs</files>
  <behavior>
    - Test: build_args_omits_mcp_when_empty -- calling build_agent_command_args with empty mcp_config_path produces args that do NOT contain "--mcp-config" string
    - Test: build_args_includes_mcp_config_when_present -- calling with "/tmp/mcp.json" produces args containing both "--mcp-config" and "/tmp/mcp.json" (existing test, verify it still passes)
    - Test: config_default_values -- allowed_tools now includes "glass_query_trend,glass_query_drill" alongside existing tools
  </behavior>
  <action>
1. In `build_agent_command_args`, replace the unconditional `--mcp-config` + `mcp_config_path` push with a conditional block: only push both strings when `!mcp_config_path.is_empty()`. Keep `--allowedTools` and `--dangerously-skip-permissions` unconditional.

2. Update `AgentRuntimeConfig::default()` to change `allowed_tools` from `"glass_query,glass_context,Bash,Read"` to `"glass_query,glass_query_trend,glass_query_drill,glass_context,Bash,Read"`. These tools exist (Phase 53) but were not in the default list.

3. Add a new test `build_args_omits_mcp_when_empty` that calls `build_agent_command_args` with `""` as mcp_config_path and asserts the resulting Vec does NOT contain `"--mcp-config"`.

4. Update the existing `config_default_values` test assertion to match the new default allowed_tools string.

5. Update the doc comment on `build_agent_command_args` to note that `--mcp-config` is only included when path is non-empty.
  </action>
  <verify>
    <automated>cd /c/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo test -p glass_core --lib agent_runtime -- -q</automated>
  </verify>
  <done>build_agent_command_args conditionally emits --mcp-config, default allowed_tools includes SOI query tools, all tests pass including new empty-path test</done>
</task>

<task type="auto">
  <name>Task 2: Write MCP config JSON and call flush_collapsed at shutdown sites</name>
  <files>src/main.rs</files>
  <action>
1. **MCP config generation in try_spawn_agent** (around line 734): Replace the empty string `""` with a real MCP config path. Before building command args:
   - Resolve Glass binary path via `std::env::current_exe()`
   - Construct `glass_dir.join("agent-mcp.json")` path
   - Write JSON with `serde_json::json!({ "mcpServers": { "glass": { "command": exe_path, "args": ["mcp", "serve"] } } })` using `std::fs::write`
   - If current_exe() or write fails, log warning and use empty string (graceful degradation)
   - Pass the config path string to `build_agent_command_args`
   - Remove the `for arg in &args { if !arg.is_empty() { cmd.arg(arg); } }` loop and replace with `cmd.args(&args)` since build_agent_command_args now handles empty paths correctly

2. **flush_collapsed at ConfigReloaded shutdown** (around line 3903): Before `self.agent_runtime = None`, insert:
   ```rust
   if let Some(event) = self.activity_filter.flush_collapsed() {
       if let Some(tx) = &self.activity_stream_tx {
           let _ = tx.try_send(event);
       }
   }
   ```

3. **flush_collapsed at AgentCrashed shutdown** (around line 4263, the `else` branch where restart limit exceeded): Before `self.agent_runtime = None`, insert the same flush_collapsed + try_send block.

4. **Update comment** at the try_spawn_agent call site: Remove "no MCP config for Phase 56" comment and replace with a comment explaining the MCP config JSON generation.
  </action>
  <verify>
    <automated>cd /c/Users/nkngu/apps/Glass && export PATH="/c/Users/nkngu/.cargo/bin:$PATH" && cargo build 2>&1 && cargo clippy --workspace -- -D warnings 2>&1</automated>
  </verify>
  <done>Agent subprocess spawned with valid --mcp-config path pointing to ~/.glass/agent-mcp.json, flush_collapsed called at all three agent shutdown sites (ConfigReloaded, AgentCrashed limit exceeded, window close via Drop), no clippy warnings</done>
</task>

</tasks>

<verification>
1. `cargo test --workspace` -- all existing tests pass (no regressions)
2. `cargo clippy --workspace -- -D warnings` -- zero warnings
3. `cargo test -p glass_core --lib agent_runtime::tests::build_args_omits_mcp_when_empty` -- new test passes
4. Grep for dangling `--mcp-config` without guard: `grep -n "mcp-config" crates/glass_core/src/agent_runtime.rs` shows conditional emission only
</verification>

<success_criteria>
- build_agent_command_args with empty path produces NO --mcp-config in args
- build_agent_command_args with valid path produces --mcp-config followed by that path
- try_spawn_agent writes ~/.glass/agent-mcp.json with correct mcpServers schema before spawn
- flush_collapsed() called at ConfigReloaded and AgentCrashed shutdown paths
- Default allowed_tools includes glass_query_trend and glass_query_drill
- All workspace tests pass, zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/61-wire-mcp-config-to-agent/61-01-SUMMARY.md`
</output>
