---
phase: 06-output-capture-writer-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_renderer/src/frame.rs
autonomous: false
requirements: [INFR-02]

must_haves:
  truths:
    - "Block decorations (separator lines, exit code badges) render at correct positions during scrollback"
    - "display_offset=0 (no scroll) still works identically to before"
  artifacts:
    - path: "crates/glass_renderer/src/frame.rs"
      provides: "Real display_offset passed to block_renderer and block_text"
      contains: "snapshot.display_offset"
  key_links:
    - from: "crates/glass_renderer/src/frame.rs"
      to: "crates/glass_terminal/src/grid_snapshot.rs"
      via: "snapshot.display_offset field"
      pattern: "snapshot\\.display_offset"
---

<objective>
Replace hardcoded `display_offset = 0` in frame.rs with the real value from GridSnapshot so block decorations scroll correctly with terminal content.

Purpose: Fixes tech debt (INFR-02) that would cause block decorations to stay pinned to wrong positions during scrollback. Prerequisite for Phase 8 search navigation.
Output: Two lines changed in frame.rs.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-output-capture-writer-integration/06-RESEARCH.md

<interfaces>
From crates/glass_terminal/src/grid_snapshot.rs:
```rust
pub struct GridSnapshot {
    // ...
    pub display_offset: usize,  // Already captured at line 196
    // ...
}
```

From crates/glass_renderer/src/frame.rs (lines 115 and 169):
```rust
// Line 115: let display_offset = 0; // TODO: wired in Plan 04 from scrollback offset
// Line 169: let display_offset = 0; // TODO: wired in Plan 04
```

From crates/glass_renderer/src/block_renderer.rs:
```rust
// build_block_rects and build_block_text already accept display_offset: usize parameter
// The filter logic inside already uses display_offset correctly -- it was designed for this
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire snapshot.display_offset through frame.rs</name>
  <files>crates/glass_renderer/src/frame.rs</files>
  <action>
    Replace exactly two lines in `crates/glass_renderer/src/frame.rs`:

    Line 115 (inside the `if !blocks.is_empty()` block for rect building):
    BEFORE: `let display_offset = 0; // TODO: wired in Plan 04 from scrollback offset`
    AFTER:  `let display_offset = snapshot.display_offset;`

    Line 169 (inside the `if !blocks.is_empty()` block for text building):
    BEFORE: `let display_offset = 0; // TODO: wired in Plan 04`
    AFTER:  `let display_offset = snapshot.display_offset;`

    That is the entire change. No other files need modification. The `snapshot` parameter is already available in `draw_frame()` (it is the `&GridSnapshot` parameter at line 103). The `display_offset` field is already `pub` on `GridSnapshot` and is already populated from the terminal state at capture time.

    Understanding the convention (from RESEARCH.md Pitfall 6): `display_offset = 0` means "showing latest content" (bottom of scrollback). Larger values mean scrolled further back. `BlockRenderer` visibility logic already handles this correctly -- it was designed to receive the real offset.
  </action>
  <verify>
    <automated>cargo build -p glass_renderer</automated>
  </verify>
  <done>Both hardcoded `display_offset = 0` lines replaced with `snapshot.display_offset`. Code compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify block decorations scroll correctly</name>
  <files>crates/glass_renderer/src/frame.rs</files>
  <action>
    Human verifies that block decorations (separator lines, exit code badges) now scroll with terminal content during scrollback navigation, instead of staying pinned to fixed screen positions.
  </action>
  <verify>
    <automated>cargo build -p glass_renderer</automated>
  </verify>
  <done>User confirms decorations scroll correctly with terminal content and no regression in non-scrolled display.</done>
  <what-built>Block decorations now use real display_offset from terminal scrollback state instead of hardcoded 0. Separator lines and exit code badges should scroll with terminal content.</what-built>
  <how-to-verify>
    1. Run Glass terminal (`cargo run`)
    2. Execute several commands (e.g., `echo hello`, `ls`, `echo world`, `false`)
    3. Verify block decorations (separator lines between commands, exit code badges) appear correctly at the bottom (no scroll)
    4. Scroll UP through the terminal history using mouse wheel or Shift+PageUp
    5. Verify that separator lines and exit code badges move with their commands -- they should NOT stay pinned to fixed screen positions
    6. Scroll back DOWN to the bottom
    7. Verify everything looks normal again (same as step 3)
  </how-to-verify>
  <resume-signal>Type "approved" if decorations scroll correctly, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
cargo build -p glass_renderer -- compiles
Visual verification: block decorations scroll with content during scrollback navigation
</verification>

<success_criteria>
- frame.rs uses snapshot.display_offset instead of hardcoded 0
- Block decorations visually scroll with terminal content (human verified)
- No regression in default (non-scrolled) display
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-capture-writer-integration/06-03-SUMMARY.md`
</output>
