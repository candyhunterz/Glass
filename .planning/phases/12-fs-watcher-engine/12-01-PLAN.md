---
phase: 12-fs-watcher-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/glass_snapshot/Cargo.toml
  - crates/glass_snapshot/src/lib.rs
  - crates/glass_snapshot/src/types.rs
  - crates/glass_snapshot/src/ignore_rules.rs
  - crates/glass_snapshot/src/watcher.rs
autonomous: true
requirements: [SNAP-04, STOR-02]

must_haves:
  truths:
    - "IgnoreRules excludes .git/, node_modules/, and target/ by default"
    - "IgnoreRules loads user-defined patterns from .glassignore file"
    - "FsWatcher detects file create, modify, delete, and rename events in a watched directory"
    - "FsWatcher filters out ignored paths before returning events"
    - "Events can be drained from FsWatcher after filesystem activity"
  artifacts:
    - path: "crates/glass_snapshot/src/ignore_rules.rs"
      provides: "IgnoreRules struct with load() and is_ignored()"
      exports: ["IgnoreRules"]
    - path: "crates/glass_snapshot/src/watcher.rs"
      provides: "FsWatcher struct with new(), drain_events()"
      exports: ["FsWatcher"]
    - path: "crates/glass_snapshot/src/types.rs"
      provides: "WatcherEvent and WatcherEventKind types"
      exports: ["WatcherEvent", "WatcherEventKind"]
  key_links:
    - from: "crates/glass_snapshot/src/watcher.rs"
      to: "crates/glass_snapshot/src/ignore_rules.rs"
      via: "FsWatcher holds IgnoreRules and calls is_ignored() in drain_events()"
      pattern: "self\\.ignore\\.is_ignored"
    - from: "crates/glass_snapshot/src/watcher.rs"
      to: "crates/glass_snapshot/src/types.rs"
      via: "WatcherEvent::from_notify converts notify events"
      pattern: "WatcherEvent::from_notify"
---

<objective>
Build the FS watcher library: IgnoreRules for .glassignore pattern matching and FsWatcher for filesystem event monitoring during command execution.

Purpose: Provides the core watcher engine that Phase 12 Plan 02 will wire into main.rs. Separating library from integration keeps each plan focused and testable.
Output: Two new modules in glass_snapshot crate (ignore_rules.rs, watcher.rs) with full test coverage.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fs-watcher-engine/12-RESEARCH.md

<interfaces>
<!-- Existing types in glass_snapshot that the new modules will use -->

From crates/glass_snapshot/src/types.rs:
```rust
pub enum Confidence { High, Low, ReadOnly }
pub struct ParseResult { pub targets: Vec<PathBuf>, pub confidence: Confidence }
pub struct SnapshotRecord { pub id: i64, pub command_id: i64, pub cwd: String, pub created_at: i64 }
pub struct SnapshotFileRecord { pub id: i64, pub snapshot_id: i64, pub file_path: String, pub blob_hash: Option<String>, pub file_size: Option<u64>, pub source: String }
```

From crates/glass_snapshot/src/lib.rs:
```rust
pub struct SnapshotStore { db: SnapshotDb, blobs: BlobStore }
impl SnapshotStore {
    pub fn open(glass_dir: &Path) -> Result<Self>;
    pub fn create_snapshot(&self, command_id: i64, cwd: &str) -> Result<i64>;
    pub fn store_file(&self, snapshot_id: i64, path: &Path, source: &str) -> Result<()>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependencies and implement IgnoreRules</name>
  <files>crates/glass_snapshot/Cargo.toml, crates/glass_snapshot/src/ignore_rules.rs, crates/glass_snapshot/src/lib.rs</files>
  <behavior>
    - Test: IgnoreRules::load with no .glassignore file excludes .git/, node_modules/, target/ by default
    - Test: Path inside .git/objects/abc is ignored
    - Test: Path inside node_modules/foo/bar.js is ignored
    - Test: Path inside target/debug/build is ignored
    - Test: Regular file path like src/main.rs is NOT ignored
    - Test: IgnoreRules::load with .glassignore file containing "*.log" ignores foo.log
    - Test: .glassignore negation pattern "!important.log" un-ignores that specific file
    - Test: .glassignore directory pattern "build/" ignores build/output.js
  </behavior>
  <action>
    1. Add `notify = "8.2"` and `ignore = "0.4"` to `[dependencies]` in glass_snapshot/Cargo.toml (NOT workspace deps -- these are snapshot-specific).
    2. Create `crates/glass_snapshot/src/ignore_rules.rs` with:
       - `pub struct IgnoreRules` wrapping `ignore::gitignore::Gitignore`
       - `pub fn load(cwd: &Path) -> Self` that:
         - Creates GitignoreBuilder rooted at cwd
         - Adds hardcoded lines: ".git/", "node_modules/", "target/"
         - Checks for cwd.join(".glassignore"), calls builder.add() if it exists
         - Builds matcher with fallback to empty matcher on error
       - `pub fn is_ignored(&self, path: &Path) -> bool` that calls `self.matcher.matched(path, path.is_dir()).is_ignore()`
    3. Add `pub mod ignore_rules;` to lib.rs and re-export: `pub use ignore_rules::IgnoreRules;`
    4. Write tests in `ignore_rules::tests` module using tempdir for .glassignore file tests.

    Use the `ignore` crate's gitignore module per research recommendation. Do NOT hand-roll glob matching.
  </action>
  <verify>
    <automated>cargo test -p glass_snapshot ignore_rules::tests -- --nocapture</automated>
  </verify>
  <done>IgnoreRules correctly excludes hardcoded directories and loads .glassignore patterns. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FsWatcher with WatcherEvent types</name>
  <files>crates/glass_snapshot/src/types.rs, crates/glass_snapshot/src/watcher.rs, crates/glass_snapshot/src/lib.rs</files>
  <behavior>
    - Test: WatcherEvent::from_notify converts Create event to WatcherEventKind::Create
    - Test: WatcherEvent::from_notify converts Modify(Data) event to WatcherEventKind::Modify
    - Test: WatcherEvent::from_notify converts Remove event to WatcherEventKind::Delete
    - Test: WatcherEvent::from_notify returns None for Access events (not a modification)
    - Test: FsWatcher::new successfully watches a temp directory
    - Test: Creating a file in watched dir produces a Create event in drain_events()
    - Test: Modifying a file in watched dir produces a Modify event in drain_events()
    - Test: Deleting a file in watched dir produces a Delete event in drain_events()
    - Test: Events for ignored paths (e.g., inside node_modules/) are filtered out by drain_events()
    - Test: drain_events() deduplicates multiple events for the same path
  </behavior>
  <action>
    1. Add to `crates/glass_snapshot/src/types.rs`:
       ```rust
       #[derive(Debug, Clone)]
       pub struct WatcherEvent {
           pub path: PathBuf,
           pub kind: WatcherEventKind,
       }

       #[derive(Debug, Clone, PartialEq)]
       pub enum WatcherEventKind {
           Create,
           Modify,
           Delete,
           Rename { to: PathBuf },
       }
       ```
       Add `impl WatcherEvent { pub fn from_notify(event: &notify::Event, path: &Path) -> Option<Self> }` following the research pattern -- match on EventKind::Create, Modify(Data), Modify(Name(RenameMode::Both)), Remove. Return None for Access/Other/Metadata-only.

    2. Create `crates/glass_snapshot/src/watcher.rs` with:
       - `pub struct FsWatcher` containing: `_watcher: RecommendedWatcher`, `rx: mpsc::Receiver<Result<Event, notify::Error>>`, `ignore: IgnoreRules`
       - `pub fn new(cwd: &Path, ignore: IgnoreRules) -> anyhow::Result<Self>` -- creates channel, creates recommended_watcher with callback that sends to tx, watches cwd with RecursiveMode::Recursive. Configure with `Config::default().with_follow_symlinks(false)` to avoid symlink loops per research pitfall #6.
       - `pub fn drain_events(&self) -> Vec<WatcherEvent>` -- try_recv loop, filter with is_ignored, convert via WatcherEvent::from_notify, deduplicate using HashSet<PathBuf> (keep last event per path).

    3. Update lib.rs: add `pub mod watcher;` and re-exports: `pub use watcher::FsWatcher;` and `pub use types::{WatcherEvent, WatcherEventKind};`

    4. Write tests in `watcher::tests` using tempdir. For FS event tests, add a small `std::thread::sleep(Duration::from_millis(100))` after file operations to give notify time to deliver events. Keep sleeps minimal.

    Note on notify Config: Use `notify::Config::default()` and call `.with_follow_symlinks(false)` if the method exists in v8.2. If not available, document and skip -- the existing SnapshotStore already skips symlinks during storage.
  </action>
  <verify>
    <automated>cargo test -p glass_snapshot watcher::tests -- --nocapture</automated>
  </verify>
  <done>FsWatcher watches a directory, captures create/modify/delete/rename events, filters ignored paths, and deduplicates. All tests pass including integration with IgnoreRules from Task 1.</done>
</task>

</tasks>

<verification>
cargo test -p glass_snapshot -- --nocapture
cargo test --workspace
</verification>

<success_criteria>
- IgnoreRules excludes .git, node_modules, target by default and loads .glassignore
- FsWatcher detects file create/modify/delete/rename events
- Ignored paths are filtered from watcher output
- Events are deduplicated per path
- All tests pass, workspace builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-fs-watcher-engine/12-01-SUMMARY.md`
</output>
