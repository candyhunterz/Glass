---
phase: 26-performance-profiling-optimization
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - PERFORMANCE.md
  - crates/glass_terminal/src/grid_snapshot.rs
autonomous: false
requirements: [PERF-03]

must_haves:
  truths:
    - "PERFORMANCE.md exists at repo root with measured cold start, input latency, and idle memory values"
    - "All three metrics meet documented targets: cold start <500ms, input latency <5ms, idle memory <120MB"
    - "Criterion benchmark results are recorded in PERFORMANCE.md with mean and std dev"
  artifacts:
    - path: "PERFORMANCE.md"
      provides: "Committed performance baseline with measured values and criterion results"
      contains: "Cold start"
    - path: "crates/glass_terminal/src/grid_snapshot.rs"
      provides: "Optimized snapshot_term with Vec pre-allocation"
      contains: "Vec::with_capacity"
  key_links:
    - from: "PERFORMANCE.md"
      to: "benches/perf_benchmarks.rs"
      via: "Documents criterion benchmark names and results"
      pattern: "cargo bench"
    - from: "PERFORMANCE.md"
      to: "src/main.rs"
      via: "Documents PERF log metrics from cold_start and memory_stats"
      pattern: "PERF"
---

<objective>
Run profiling, apply targeted optimizations, and document the performance baseline.

Purpose: Use the benchmark infrastructure from Plan 01 to measure current performance, apply data-driven optimizations where beneficial, and commit a PERFORMANCE.md baseline for future regression comparison (PERF-03).
Output: PERFORMANCE.md with measured values meeting targets, and any optimization improvements committed.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-performance-profiling-optimization/26-RESEARCH.md
@.planning/phases/26-performance-profiling-optimization/26-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
Benchmark groups available after Plan 01:
- `resolve_color_spec`, `resolve_color_named`, `resolve_color_indexed` -- color resolution micro-benchmarks
- `osc_scan_mixed` -- OscScanner parsing benchmark
- `cold_start_help` -- process spawn startup benchmark

Existing PERF logging (src/main.rs):
- Line 743: `tracing::info!("PERF cold_start={:?}", self.cold_start.elapsed());` -- at first frame
- Line 744-746: `memory_stats::memory_stats()` -- idle physical memory
- Line 1207-1215: `Instant::now()` key latency measurement

Known v1.0 baseline (from PROJECT.md):
- Cold start: 360ms (target <500ms)
- Key latency: 3-7us (target <5ms)
- Idle memory: 86MB (target <120MB)

From grid_snapshot.rs (optimization target):
```rust
pub fn snapshot_term(term: &Term<EventProxy>, defaults: &DefaultColors) -> GridSnapshot {
    // Line 173: let mut cells = Vec::new();  <-- no pre-allocation
    // Iterates content.display_iter, pushes each cell
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run benchmarks, apply optimizations, and measure results</name>
  <files>crates/glass_terminal/src/grid_snapshot.rs</files>
  <action>
1. **Run criterion benchmarks** to establish baseline:
   ```bash
   cargo bench 2>&1 | tee bench-results.txt
   ```
   Record the mean and std dev for each benchmark group.

2. **Apply snapshot_term Vec pre-allocation optimization** in `crates/glass_terminal/src/grid_snapshot.rs`:
   - Line 173: Change `let mut cells = Vec::new();` to `let mut cells = Vec::with_capacity(term.columns() * term.screen_lines());`
   - This avoids repeated Vec reallocations during the cell iteration loop (80x24 = 1920 cells, 80x50 = 4000 cells).

3. **Re-run benchmarks** after optimization to measure improvement:
   ```bash
   cargo bench 2>&1 | tee bench-results-optimized.txt
   ```

4. **Build release binary** for integration-level measurement:
   ```bash
   cargo build --release
   ```

5. **Verify existing tests pass** after optimization:
   ```bash
   cargo test
   ```

Note: Do NOT add more optimizations unless profiling reveals a specific bottleneck. The v1.0 baseline already meets all targets comfortably. The Vec pre-allocation is a clear win (eliminates reallocation in a per-frame function) and is the only optimization the research identified as clearly beneficial.
  </action>
  <verify>
    <automated>cargo bench -- --quick 2>&1 && cargo test 2>&1</automated>
  </verify>
  <done>Criterion benchmarks run successfully. snapshot_term uses Vec::with_capacity for pre-allocation. All existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify real-world performance metrics against targets</name>
  <files>none</files>
  <action>User verifies real-world performance by running Glass with PERF logging and checking metrics against targets. This requires GPU + PTY runtime which cannot be automated.</action>
  <what-built>Performance benchmarks and optimization pass. Need to verify real-world metrics by running Glass and checking PERF log output against targets (cold start less than 500ms, idle memory less than 120MB). These require GPU + PTY runtime which cannot be automated in CI.</what-built>
  <how-to-verify>
    1. Build release: `cargo build --release`
    2. Run Glass: `RUST_LOG=info target/release/glass.exe`
    3. Wait for terminal to fully render (first frame)
    4. Check terminal output for lines matching:
       - `PERF cold_start=XXXms` -- should be under 500ms
       - `PERF memory_physical=XX.XMB` -- should be under 120MB
    5. Type a few characters and note responsiveness (input latency is 3-7us from v1.0, well under 5ms target)
    6. Close Glass
    7. Report the measured values so they can be recorded in PERFORMANCE.md
  </how-to-verify>
  <verify>User confirms metrics meet targets</verify>
  <done>User has verified cold start, memory, and input latency meet targets and provided measured values.</done>
  <resume-signal>Provide measured values: cold_start=___ms, memory=___MB. Or type "approved" if using v1.0 baseline values (360ms, 86MB).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create PERFORMANCE.md baseline document</name>
  <files>PERFORMANCE.md</files>
  <action>
Create `PERFORMANCE.md` at the repo root with this structure:

```markdown
# Glass Performance Baseline

**Measured:** [current date]
**Platform:** Windows 11, Rust [version from rustc --version]
**Terminal size:** 80x24 (default)
**Build:** `cargo build --release`

## Targets

| Metric | Target | Measured | Status |
|--------|--------|----------|--------|
| Cold start (first frame) | <500ms | [value from checkpoint or 360ms] | PASS |
| Input latency (key -> render) | <5ms | [value from checkpoint or 3-7us] | PASS |
| Idle memory (physical) | <120MB | [value from checkpoint or 86MB] | PASS |

## Criterion Benchmarks

Run: `cargo bench`
Reports: `target/criterion/report/index.html`

| Benchmark | Mean | Std Dev |
|-----------|------|---------|
| resolve_color_spec | [from bench output] | [from bench output] |
| resolve_color_named | [from bench output] | [from bench output] |
| resolve_color_indexed | [from bench output] | [from bench output] |
| osc_scan_mixed | [from bench output] | [from bench output] |
| cold_start_help | [from bench output] | [from bench output] |

## Optimizations Applied

- `snapshot_term` Vec pre-allocation: `Vec::with_capacity(cols * lines)` eliminates reallocation during per-frame cell iteration

## Profiling

Run: `cargo run --release --features perf`
Output: `glass-trace.json`
View: Open in https://ui.perfetto.dev

Instrumented hot paths:
- `glass_pty_loop` -- main PTY event loop
- `pty_read_with_scan` -- PTY read + OscScanner
- `snapshot_term` -- terminal grid snapshot extraction
- `draw_frame` -- single-pane render frame
- `draw_multi_pane_frame` -- multi-pane render frame
- `build_rects` -- cell background rectangle generation
- `build_text_buffers` -- text buffer generation for glyphon
- `OscScanner::scan` -- OSC sequence scanning (trace level)

## Measurement Notes

- Cold start = wall-clock time from process exec to first frame (`self.cold_start.elapsed()` at RedrawRequested)
- Memory = physical memory from `memory_stats::memory_stats()` after first frame
- Input latency = `Instant::now()` before key encode to after event dispatch (measured in PERF trace logs)
- GPU driver allocations account for ~80MB of baseline memory
- Memory varies by GPU driver, installed fonts, and terminal size
```

Fill in the actual measured values from the criterion bench output (Task 1 results) and the checkpoint values. If checkpoint was approved with default v1.0 values, use: 360ms cold start, 3-7us latency, 86MB memory.
  </action>
  <verify>
    <automated>test -f PERFORMANCE.md && grep -q "Cold start" PERFORMANCE.md && grep -q "cargo bench" PERFORMANCE.md && echo "PASS"</automated>
  </verify>
  <done>PERFORMANCE.md exists at repo root with measured cold start, input latency, idle memory values, criterion benchmark results, profiling instructions, and optimization notes. All three metrics show PASS status against targets.</done>
</task>

</tasks>

<verification>
1. `cargo bench -- --quick` -- benchmarks still run after optimizations
2. `cargo test` -- no regressions from snapshot_term optimization
3. `test -f PERFORMANCE.md` -- baseline document committed
4. `grep -q "PASS" PERFORMANCE.md` -- all targets met
</verification>

<success_criteria>
- PERFORMANCE.md committed with measured values for all three metrics
- All metrics meet targets: cold start <500ms, input latency <5ms, idle memory <120MB
- Criterion benchmark results recorded with mean and std dev
- snapshot_term optimization (Vec::with_capacity) applied and tests passing
- Profiling instructions documented for future use
</success_criteria>

<output>
After completion, create `.planning/phases/26-performance-profiling-optimization/26-02-SUMMARY.md`
</output>
