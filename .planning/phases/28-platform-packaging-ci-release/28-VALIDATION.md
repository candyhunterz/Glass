---
phase: 28
slug: platform-packaging-ci-release
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-07
---

# Phase 28 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual validation (packaging is inherently integration/smoke testing) |
| **Config file** | none — packaging tools configured in Cargo.toml and wix/main.wxs |
| **Quick run command** | `cargo build --release` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo build --release`
- **After every plan wave:** Run `cargo test --workspace` + attempt local packaging command
- **Before `/gsd:verify-work`:** Full suite must be green + push `v0.1.0-rc.1` test tag
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 28-01-01 | 01 | 1 | PKG-01 | manual | `cargo wix --no-build` (builds MSI; actual install is manual) | ❌ W0 | ⬜ pending |
| 28-01-02 | 01 | 1 | PKG-02 | manual | `bash packaging/macos/build-dmg.sh` (builds DMG; install is manual) | ❌ W0 | ⬜ pending |
| 28-01-03 | 01 | 1 | PKG-03 | manual | `cargo deb --no-build` (builds deb; install is manual) | ❌ W0 | ⬜ pending |
| 28-02-01 | 02 | 2 | PKG-04 | smoke | Push test tag and verify workflow runs | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `wix/main.wxs` — WiX installer definition (generated by `cargo wix init`)
- [ ] `packaging/macos/Info.plist` — macOS app bundle metadata
- [ ] `packaging/macos/build-dmg.sh` — DMG creation script
- [ ] `[package.metadata.deb]` section in Cargo.toml
- [ ] `.github/workflows/release.yml` — Release workflow
- [ ] cargo-wix and cargo-deb installed locally for testing

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| MSI installs to Program Files and adds to PATH | PKG-01 | Requires actual Windows installer execution | Run MSI, check `C:\Program Files\Glass Terminal\glass.exe` exists, verify `glass` in PATH |
| DMG provides drag-to-Applications bundle | PKG-02 | Requires actual macOS DMG mounting and app launch | Open DMG, drag to Applications, launch Glass.app |
| deb package makes glass available system-wide | PKG-03 | Requires actual deb installation on Debian/Ubuntu | `sudo dpkg -i glass_*.deb && which glass` |
| v* tag triggers CI and publishes assets | PKG-04 | Requires pushing a real git tag to GitHub | Push `v0.1.0-rc.1` tag, verify 3 assets appear on GitHub Release |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
