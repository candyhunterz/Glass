---
phase: 02-terminal-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/glass_renderer/Cargo.toml
  - crates/glass_terminal/src/grid_snapshot.rs
  - crates/glass_terminal/src/lib.rs
  - crates/glass_terminal/Cargo.toml
  - crates/glass_renderer/src/glyph_cache.rs
  - crates/glass_renderer/src/lib.rs
autonomous: true
requirements: [CORE-02, CORE-05, CORE-08, RNDR-02]

must_haves:
  truths:
    - "GridSnapshot extracts all cell data (char, fg, bg, flags) from Term under a brief lock"
    - "Color resolution handles Named (with DIM/BOLD variants), Spec (truecolor), and Indexed (256-color) correctly"
    - "INVERSE flag swaps fg and bg during resolution"
    - "WIDE_CHAR_SPACER cells are marked for skip during rendering"
    - "glyphon FontSystem, TextAtlas, TextRenderer, SwashCache, Cache initialize without error"
    - "Scrollback history configured to 10,000 lines"
  artifacts:
    - path: "crates/glass_terminal/src/grid_snapshot.rs"
      provides: "GridSnapshot struct, RenderedCell struct, snapshot_term(), resolve_color()"
      exports: ["GridSnapshot", "RenderedCell", "snapshot_term"]
    - path: "crates/glass_renderer/src/glyph_cache.rs"
      provides: "GlyphCache struct wrapping glyphon FontSystem, TextAtlas, TextRenderer, SwashCache, Cache, Viewport"
      exports: ["GlyphCache"]
  key_links:
    - from: "crates/glass_terminal/src/grid_snapshot.rs"
      to: "alacritty_terminal::term::Term"
      via: "term.renderable_content()"
      pattern: "renderable_content"
    - from: "crates/glass_renderer/src/glyph_cache.rs"
      to: "glyphon"
      via: "FontSystem::new(), TextAtlas::new(), TextRenderer::new()"
      pattern: "FontSystem::new"
---

<objective>
Set up the data pipeline foundation: GridSnapshot extracts renderable cell data from the terminal grid, color resolution converts alacritty_terminal Color variants to RGB, and glyphon text rendering infrastructure is initialized. Add workspace dependencies (glyphon, arboard) and configure 10,000-line scrollback.

Purpose: Establish the contracts and data types that Plan 02 (rendering) and Plan 03 (input/clipboard) consume. Without GridSnapshot, the renderer has no data. Without GlyphCache, there is no text rendering pipeline.
Output: Compilable GridSnapshot module with color resolution, GlyphCache initialization, workspace dependencies added.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-core/02-RESEARCH.md

Source files this plan reads from or extends:
@crates/glass_terminal/src/lib.rs
@crates/glass_terminal/src/pty.rs
@crates/glass_renderer/src/surface.rs
@crates/glass_renderer/src/lib.rs
@crates/glass_core/src/config.rs
@Cargo.toml
@crates/glass_renderer/Cargo.toml
@crates/glass_terminal/Cargo.toml

<interfaces>
<!-- Existing interfaces the executor needs -->

From crates/glass_core/src/config.rs:
```rust
#[derive(Debug, Clone)]
pub struct GlassConfig {
    pub font_family: String,
    pub font_size: f32,
    pub shell: Option<String>,
}
```

From crates/glass_core/src/event.rs:
```rust
#[derive(Debug, Clone)]
pub enum AppEvent {
    TerminalDirty { window_id: winit::window::WindowId },
    SetTitle { window_id: winit::window::WindowId, title: String },
    TerminalExit { window_id: winit::window::WindowId },
}
```

From crates/glass_renderer/src/surface.rs:
```rust
pub struct GlassRenderer {
    device: wgpu::Device,
    queue: wgpu::Queue,
    surface: wgpu::Surface<'static>,
    surface_config: wgpu::SurfaceConfiguration,
}
impl GlassRenderer {
    pub async fn new(window: Arc<winit::window::Window>) -> Self;
    pub fn draw(&mut self);
    pub fn resize(&mut self, width: u32, height: u32);
}
```

From crates/glass_terminal/src/pty.rs (spawn_pty signature):
```rust
pub fn spawn_pty(event_proxy: EventProxy) -> (EventLoopSender, Arc<FairMutex<Term<EventProxy>>>)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workspace dependencies and create GridSnapshot with color resolution</name>
  <files>
    Cargo.toml,
    crates/glass_terminal/Cargo.toml,
    crates/glass_terminal/src/grid_snapshot.rs,
    crates/glass_terminal/src/lib.rs
  </files>
  <behavior>
    - Test: resolve_color with Color::Spec(Rgb{r:255,g:0,b:0}) returns Rgb{r:255,g:0,b:0} (truecolor passthrough)
    - Test: resolve_color with Color::Named(NamedColor::Blue) + Flags::BOLD returns bright blue variant
    - Test: resolve_color with Color::Named(NamedColor::White) + Flags::DIM returns dim white variant
    - Test: resolve_color with Color::Indexed(196) returns the correct 256-color palette entry
    - Test: INVERSE flag swaps fg and bg in RenderedCell
    - Test: WIDE_CHAR_SPACER cells are included in snapshot with a marker so renderer can skip them
  </behavior>
  <action>
    1. Add workspace dependencies to root Cargo.toml:
       ```toml
       glyphon = "0.10.0"
       arboard = "3"
       ```

    2. Add `arboard.workspace = true` to crates/glass_terminal/Cargo.toml (needed for clipboard in Plan 03, but declare dependency now so it compiles).

    3. Create `crates/glass_terminal/src/grid_snapshot.rs` with:
       - `DefaultColors` struct holding default fg (Rgb{r:204,g:204,b:204}) and bg (Rgb{r:26,g:26,b:26}) — matching the dark gray from GlassRenderer clear color.
       - `RenderedCell` struct: `point: Point, c: char, fg: Rgb, bg: Rgb, flags: Flags, zerowidth: Vec<char>`
       - `GridSnapshot` struct: `cells: Vec<RenderedCell>, cursor: RenderableCursor, display_offset: usize, mode: TermMode, columns: usize, screen_lines: usize`
       - `resolve_color(color: Color, colors: &Colors, defaults: &DefaultColors, flags: Flags) -> Rgb` function handling:
         - `Color::Spec(rgb)` -> direct passthrough
         - `Color::Named(name)` -> check BOLD for to_bright(), DIM for to_dim(), lookup in Colors or fall back to default palette
         - `Color::Indexed(idx)` -> lookup in Colors or fall back to default_indexed_color(idx)
       - `default_indexed_color(idx: u8) -> Rgb` function implementing the standard 256-color palette:
         - 0-15: standard ANSI colors (use the xterm defaults)
         - 16-231: 6x6x6 color cube (r*36 + g*6 + b + 16, each component 0-5 mapped to 0,95,135,175,215,255)
         - 232-255: grayscale ramp (8 + 10*n for n=0..23)
       - `snapshot_term(term: &Term<EventProxy>, defaults: &DefaultColors) -> GridSnapshot` function that:
         - Calls `term.renderable_content()`
         - Iterates `content.display_iter`, resolving fg/bg colors
         - Applies INVERSE flag swap AFTER resolution
         - Collects zerowidth chars from cell extras
         - Returns completed GridSnapshot

    4. Update `crates/glass_terminal/src/lib.rs` to add `pub mod grid_snapshot;` and `pub use grid_snapshot::{GridSnapshot, RenderedCell, snapshot_term, DefaultColors};`

    5. Update `crates/glass_terminal/src/pty.rs`: change the Term config to set `scrolling_history: 10_000` (CORE-05). Currently it uses default. Add `use alacritty_terminal::term::Config as TermConfig;` and pass the config to `Term::new()`.

    Research reference for color resolution: see Pattern 2 in 02-RESEARCH.md. For Cell access: see "alacritty_terminal Cell Access" code example. Check actual alacritty_terminal 0.25.1 source for exact field names — the research is HIGH confidence but executor should verify against compiler output.

    IMPORTANT: The `renderable_content()` API returns `RenderableContent` which has `display_iter` (an iterator of `RenderableCell`), `cursor` (RenderableCursor), `colors` (Colors reference), `mode` (TermMode), and `display_offset`. Verify exact field names against the installed crate source if compiler errors arise.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo test -p glass_terminal -- grid_snapshot --nocapture</automated>
  </verify>
  <done>
    GridSnapshot and RenderedCell types exported from glass_terminal. resolve_color handles all three Color variants with DIM/BOLD/INVERSE. 256-color palette implemented. Scrollback configured to 10,000 lines. All color resolution tests pass. `cargo build --workspace` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GlyphCache with glyphon initialization</name>
  <files>
    crates/glass_renderer/Cargo.toml,
    crates/glass_renderer/src/glyph_cache.rs,
    crates/glass_renderer/src/lib.rs
  </files>
  <action>
    1. Add `glyphon.workspace = true` to `crates/glass_renderer/Cargo.toml`.

    2. Create `crates/glass_renderer/src/glyph_cache.rs` with a `GlyphCache` struct that wraps all glyphon state needed for text rendering:
       ```rust
       pub struct GlyphCache {
           pub font_system: FontSystem,
           pub swash_cache: SwashCache,
           pub cache: Cache,
           pub atlas: TextAtlas,
           pub text_renderer: TextRenderer,
           pub viewport: Viewport,
       }
       ```

       Implement `GlyphCache::new(device: &wgpu::Device, queue: &wgpu::Queue, surface_format: wgpu::TextureFormat) -> Self`:
       - `FontSystem::new()` — discovers system fonts automatically
       - `SwashCache::new()`
       - `Cache::new(device)` — shared GPU pipelines
       - `TextAtlas::new(device, queue, &cache, surface_format)` — glyph atlas texture
       - `TextRenderer::new(&mut atlas, device, MultisampleState::default(), None)` — text draw pipeline
       - `Viewport::new(device, &cache)` — resolution-aware viewport

       Implement `GlyphCache::trim(&mut self)`:
       - Calls `self.atlas.trim()` to free unused atlas space between frames.

       Use these imports from glyphon:
       ```rust
       use glyphon::{
           Cache, FontSystem, SwashCache, TextAtlas, TextRenderer, Viewport,
       };
       use wgpu::MultisampleState;
       ```

       Reference: Pattern 3 "Glyphon Text Rendering Pipeline" in 02-RESEARCH.md for initialization sequence. The hello-world.rs example from glyphon GitHub confirms the exact API.

       NOTE: If glyphon 0.10.0 API differs from research (e.g., TextRenderer::new signature), adapt to what compiles. The research is HIGH confidence but minor signature differences are possible.

    3. Update `crates/glass_renderer/src/lib.rs`:
       - Add `pub mod glyph_cache;`
       - Add `pub use glyph_cache::GlyphCache;`

    4. Verify the full workspace compiles: `cargo build --workspace`
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
    GlyphCache struct exported from glass_renderer. FontSystem, TextAtlas, TextRenderer, SwashCache, Cache, Viewport all initialized. `cargo build --workspace` succeeds with zero errors.
  </done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds (all crates compile with new dependencies)
- `cargo test -p glass_terminal -- grid_snapshot` passes (color resolution tests)
- `cargo test --workspace` passes (no regressions from Phase 1 tests)
- GridSnapshot, RenderedCell, GlyphCache types are exported and available for Plan 02
</verification>

<success_criteria>
- glyphon 0.10.0 and arboard 3.x added to workspace dependencies
- GridSnapshot correctly extracts cell data with resolved RGB colors
- Color resolution handles Spec/Named/Indexed variants with DIM/BOLD/INVERSE
- 256-color palette implemented for Color::Indexed
- GlyphCache wraps all glyphon state needed for text rendering
- Scrollback history set to 10,000 lines in Term config
- All tests pass, workspace compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-core/02-01-SUMMARY.md`
</output>
