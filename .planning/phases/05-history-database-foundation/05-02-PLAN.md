---
phase: 05-history-database-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.rs
  - Cargo.toml
autonomous: false
requirements: [INFR-01]

must_haves:
  truths:
    - "Running `glass` with no arguments launches the terminal GUI (current behavior preserved)"
    - "Running `glass history` prints a stub message and exits with code 1"
    - "Running `glass mcp serve` prints a stub message and exits with code 1"
    - "Running `glass --help` shows subcommand help text"
  artifacts:
    - path: "src/main.rs"
      provides: "clap-based subcommand routing with Option<Commands>"
      contains: "Cli::parse"
    - path: "Cargo.toml"
      provides: "clap and glass_history dependencies for root binary"
      contains: "clap"
  key_links:
    - from: "src/main.rs"
      to: "clap derive macros"
      via: "Cli::parse() dispatches to terminal or subcommand handler"
      pattern: "match cli.command"
---

<objective>
Add clap-based subcommand routing to the Glass binary so `glass history` and `glass mcp serve` route to stub handlers while `glass` with no arguments continues to launch the terminal.

Purpose: INFR-01 requires subcommand routing as the entry point for Phase 7 (CLI queries) and Phase 9 (MCP server). This must preserve the zero-argument terminal launch behavior.
Output: Modified main.rs with clap CLI struct, subcommand enum, and routing logic.
</objective>

<execution_context>
@C:/Users/nkngu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nkngu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-history-database-foundation/05-RESEARCH.md
@src/main.rs

Key patterns from research:
- Use clap derive with Option<Subcommand> so None = launch terminal
- Parse clap FIRST, then only start winit event loop if no subcommand
- winit does not use argv on Windows, so no conflict

Existing main.rs structure:
- main() initializes tracing, loads config, creates EventLoop, runs Processor
- All GUI code is inside Processor::resumed() and event handlers
- The function `main()` needs to be split: parse CLI first, then branch
</context>

<interfaces>
<!-- From src/main.rs -- the current main() function structure that must be refactored -->
<!-- Lines 468-509: main() does tracing init, config load, event loop creation, processor creation, run_app -->
<!-- The entire block from EventLoop creation through run_app must move into a run_terminal() function -->
<!-- Tracing init and config load should stay in main() (needed by all subcommands) -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add clap subcommand routing to main.rs</name>
  <files>src/main.rs, Cargo.toml</files>
  <behavior>
    - test_no_subcommand_is_none: Cli::try_parse_from(["glass"]) yields command = None (terminal mode)
    - test_history_subcommand: Cli::try_parse_from(["glass", "history"]) yields Some(Commands::History)
    - test_mcp_serve_subcommand: Cli::try_parse_from(["glass", "mcp", "serve"]) yields Some(Commands::Mcp { action: McpAction::Serve })
    - test_help_flag: Cli::try_parse_from(["glass", "--help"]) returns help error (expected clap behavior)
    - test_unknown_subcommand_errors: Cli::try_parse_from(["glass", "bogus"]) returns error
  </behavior>
  <action>
    1. Add dependencies to root Cargo.toml [dependencies] section:
       - clap = { workspace = true }
       - glass_history = { path = "crates/glass_history" }

    2. In main.rs, add clap imports and define CLI structs at the top (after existing imports):

       ```rust
       use clap::{Parser, Subcommand};

       #[derive(Parser)]
       #[command(name = "glass", version, about = "GPU-accelerated terminal emulator")]
       struct Cli {
           #[command(subcommand)]
           command: Option<Commands>,
       }

       #[derive(Subcommand, Debug, PartialEq)]
       enum Commands {
           /// Query command history
           History,
           /// MCP server commands
           Mcp {
               #[command(subcommand)]
               action: McpAction,
           },
       }

       #[derive(Subcommand, Debug, PartialEq)]
       enum McpAction {
           /// Start the MCP server over stdio
           Serve,
       }
       ```

       Derive PartialEq on Commands and McpAction for testability.

    3. Refactor main() function:
       a. Keep UTF-8 console setup, tracing init, and config load at the top of main()
       b. Add `let cli = Cli::parse();` after tracing init
       c. Add match on cli.command:
          - None => run the existing terminal code (EventLoop creation through run_app)
          - Some(Commands::History) => { eprintln!("glass history: not yet implemented (Phase 7)"); std::process::exit(1); }
          - Some(Commands::Mcp { action: McpAction::Serve }) => { eprintln!("glass mcp serve: not yet implemented (Phase 9)"); std::process::exit(1); }
       d. Move the EventLoop + Processor + run_app block into the None arm (or into a separate run_terminal() function for cleanliness)

    4. Add tests in src/tests.rs (or the existing #[cfg(test)] mod tests):
       - Test Cli::try_parse_from with various argument combinations
       - These are unit tests on the clap struct, not integration tests requiring a running binary

    IMPORTANT: The clap parse MUST happen before EventLoop creation. If clap is parsed after EventLoop::build(), the terminal window will flash briefly even for `glass history`.

    IMPORTANT: Keep the cold_start timer. Move it above Cli::parse() so it still measures from program start.
  </action>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo test -p glass -- subcommand --nocapture 2>&1 && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Cli::try_parse_from(["glass"]) returns None (terminal mode)
    - Cli::try_parse_from(["glass", "history"]) returns Some(Commands::History)
    - Cli::try_parse_from(["glass", "mcp", "serve"]) returns Some(Commands::Mcp { action: McpAction::Serve })
    - Unknown subcommands produce clap errors
    - `cargo build` succeeds with no warnings
    - Running `cargo run` still launches the terminal (manual verification)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify subcommand routing works end-to-end</name>
  <files>src/main.rs</files>
  <action>Human verifies that the clap routing works correctly for all three modes: terminal (no args), history stub, and mcp serve stub.</action>
  <what-built>Clap subcommand routing added to glass binary. Terminal launch preserved for no-args, stub handlers for history and mcp serve.</what-built>
  <how-to-verify>
    1. Run `cargo run` -- verify terminal window opens normally (no regression)
    2. Run `cargo run -- history` -- verify it prints "glass history: not yet implemented (Phase 7)" and exits
    3. Run `cargo run -- mcp serve` -- verify it prints "glass mcp serve: not yet implemented (Phase 9)" and exits
    4. Run `cargo run -- --help` -- verify help text shows glass subcommands
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/nkngu/apps/Glass && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>User confirms terminal launches normally, subcommands route to stubs, help text displays correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo test -p glass` -- subcommand routing tests pass
- `cargo test --workspace` -- no regressions
- `cargo run` -- terminal launches
- `cargo run -- history` -- stub message + exit 1
- `cargo run -- mcp serve` -- stub message + exit 1
</verification>

<success_criteria>
- glass binary parses subcommands via clap before creating the event loop
- No arguments = terminal launch (existing behavior preserved exactly)
- `glass history` and `glass mcp serve` route to stub handlers
- All workspace tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-history-database-foundation/05-02-SUMMARY.md`
</output>
