//! Viewport layout (stub for Phase 24).

/// Rectangle describing a pane's position and size within the window.
pub struct ViewportLayout {
    /// X offset in pixels.
    pub x: u32,
    /// Y offset in pixels.
    pub y: u32,
    /// Width in pixels.
    pub width: u32,
    /// Height in pixels.
    pub height: u32,
}
